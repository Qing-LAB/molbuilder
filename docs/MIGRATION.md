# Migration ledger — old_docs/ → docs/

## Wave plan

**Sequencing decision (user, 2026-07-26): the documentation migration
completes IN FULL before any system/feature work resumes** (including the
jobset web-merge Phase 1). Docs first, then the system.

Every wave, every doc: the [README migration protocol + editorial rules
E1–E4](?doc=README.md) apply in full — structure-first merging, Mermaid where a
picture explains, plain language with full rigor, scientific foundations
preserved and enriched, and the master index updated in the same commit as
every move.

### Order: components first, the summary map last (user, 2026-07-26)

The two summary docs — `design.md` (the concise outline that *points at* the
detailed docs) and `architecture.md` (the reuse map over the subsystems) —
**aggregate every component**, so they are composed **LAST**, once the
component docs they summarize have settled. Writing them first would mean
drawing the map before walking the territory: the summary would go stale the
moment the first component moved. So the order is **bottom-up** — the L1 data
model, then the layers that build on it, then the surfaces — and the spine
summary is the final pass over the finished tree. `roadmap.md` (the forward
*plan*, which leads rather than summarizes) is the one spine doc done early.

### Keep-and-mark: old_docs survives until the final cross-check (user, 2026-07-26)

Migrated old docs are **NOT deleted** during the migration. Each worked-on
file is **kept in `old_docs/` and renamed with the `_migrated_` prefix**
(e.g. `protocols/web-api.md` → `protocols/_migrated_web-api.md`). The frozen
tree therefore stays complete until closeout, when the new tree is
cross-checked against it to prove nothing was dropped in a reconcile/merge —
*then* `old_docs/` is deleted. The ledger records the outcome; the prefix is
the at-a-glance filesystem mirror. Enforced by `tests/test_docs_structure.py`
(prefix ⇔ non-`pending` status). *(Grandfathered exception: the Wave-H
archives were relocated verbatim before this policy, so their old files are
already gone — identity-cross-checkable in `docs/archive/`.)*

| Wave | Scope (~docs) | Status |
|---|---|---|
| 0 | Freeze + skeleton + ledger + rules + tests | **done** (2162e44, 6efb0ca) |
| H | ALL historical archives → `archive/` (+ `audits/`) | **done** (a3f9b82) |
| 1 | `roadmap.md` — THE plan (absorbs all scattered phasing); the forward north-star | **done** (8143bcf) |
| 2 | `model/` (~11): structure family, periodicity, annotations, sidecars, selection, region labels, chemistry, parse stack, data vocabulary; `types/` folds in (`parsers.md` → archive) | **done** (2026-07-26) — `overview` + `structure` (+`-periodicity`/`-annotations`/`-molstruct`) + `chemistry` + `parse`; each migrated **and** fresh-eyes audited. `parsers.md` archived. Split sources still **pending** their non-model halves (claimed later): `region-labels` + `sidecar-contract` → engines; `data-vocabulary` → execution. `selection.md` → web (projects-sidebar). |
| 3 | `science/` (~4): validation machinery, chemistry correctness, pseudopotential standards, tuning | **done** (2026-07-26) — `overview` + `validation` + `chemistry-correctness` + `pseudopotentials`; each migrated **and** fresh-eyes audited (every finding verified vs code). Sources fully consumed + `_migrated_`: `scientific-validation.md`, `chemistry-correctness.md`, `pseudopotential-validation.md`, `science.md`. Reconciliation surfaced real code evolutions: XC family/author severity split, the dead-projector semilocal exemption, and the CLI-vs-preflight ERROR-set asymmetry (flagged for a code follow-up). **Split still pending its non-science half:** `scientific-validation.md §7.5` GPU eigensolver → engines Wave 4. |
| 4 | `engines/` (~7): SIESTA (+GPU setting folded in), PySCF, transport/TranSIESTA, builders, tuning; `transiesta-workflow` + `transport-guide` move in | **done** (2026-07-27) — shape = `overview` map + peer docs (`siesta`/`pyscf`/`transport`/`builders`/`tuning`), overview composed last. `siesta.md` + `pyscf.md` + `transport.md` + `builders.md` **done** (each fresh-eyes audited + enrichment-passed; each caught real source-vs-code drift). `transport.md` consolidated 4 sources + closed the `region-labels` split (transport half); `builders.md` reconciled the synthesis contract (audit found zero factual errors; doc corrected two stale source facts). **`siesta-gpu` is NOT a separate peer doc** — decided GPU is just a different eigensolver setting, so its setting/perf half folded into `siesta.md § 7.1` and its build/env recipe defers to ops/ (Wave 7). `tuning.md` **done** (audit found every code-tied number correct; reconciled that SIESTA staged-opt has shipped). **`overview.md` composed LAST (2026-07-27)** — the engines map + the three shared contracts (script wrapper, the UI→config→script boundary contract absorbing `sidecar-contract §1–10`, the staged-opt/non-convergence policy) + the registry; fresh-eyes audited (found + fixed real overstatements: the A/B/C notice pattern is spectra-only today; Check C is render-endpoint; stale test paths). A holistic cross-doc review before the overview reconciled the five peers (jkfit fix propagated to pyscf.md, tier-name alignment, siesta↔tuning reciprocity). **WAVE 4 COMPLETE.** Deferred to ops/ (Wave 7): the `siesta-gpu`/`scientific-validation §7.5` *build/env* recipe. |
| 5 | `execution/` (~16 sources → **4 consolidated docs**, user directive 2026-07-27: *fewer docs, not scattered*, each showing current-status + CLI-vs-web + shipped/developing/planned): **`job-contracts.md`** (the static on-disk formats: layout + script blocks + restart + handoff + vocabulary), **`running-a-job.md`** (the usable single-job path: run wrapper + `molbuilder.json` + checkpoints + the decoded-run monitoring view), **`job-system.md`** (the JobSet batch/staged/HPC framework — CLI-shipped, web-pending), **`overview.md`** (map + current→target transition, composed last) | **done** (2026-07-27) — **`job-contracts.md` done** (2026-07-27): consolidated `job-layout` + `script-contract` + `script-execution` + `bundle-contract` + `data-vocabulary`(§1–3), each reconciled vs code by a fresh-eyes agent (every claim spot-checked). Real drift caught & corrected: ATOM-METADATA is now **v4** (annotations channel; sidecar file v6, read-side accepts 3–6), not v3; `RunBundle`→**`BundleResult`** + `assemble_from_run_dir`→**`BundleDirParser.parse`** + **`BundleWriteError`** (write) vs `BundleError` (assembly); **nine** canonical topics not six (added `structure`/`pseudopotential`/`user`); wrapper stdout is run-indexed `-runN.out`/`-runN.pyscf.log` (auto-advancing) not static `.out`, activation is a configurable `conda activate` line not `conda run`, `.fdf` with ELPA/GPU re-routes to `molbuilder-siesta-gpu`; two-layer SLURM (`.run.sh` inner + `.sbatch` outer) emitted by `runwrap` and **reused** by jobset; HEADER block is reserved-but-unemitted, BENCH-MARKS is SIESTA-only, gained `Diag.Algorithm type=enum`. `job-decoder` reassigned to `running-a-job.md` (it's a monitoring concern). **`running-a-job.md` done** (2026-07-27): consolidated `job-execution` + `config` + `checkpoints-guide` + `run-checkpoints` + `job-decoder`, reconciled vs code by two fresh-eyes digest agents + a fresh-eyes audit (every load-bearing claim spot-checked). Reconciled: the standalone/detection (T/M/C/A/H) contract holds; env routing swaps to `molbuilder-siesta-gpu` for **any** ELPA `.fdf` (even CPU-ELPA); CPU rank auto-default clamps to `n_atoms` (propor IMAX=0 guard); `.sbatch` body is a one-line `bash <base>.run.sh "$@"`. Superseded/drift dropped: `scheduler.gpu.mem` (stale comment + dead example key — memory is one unified per-job path), `mbbench/` stdlib lib + `bench-manifest@1` + static `bash job-cpu.run.sh` cookbook (all superseded by the JobSet framework → `job-system.md`), `emit_sbatch=(scheduler==slurm)` → `mode==submit`. Draft-not-shipped extracted to `roadmap.md` (R3): the decoder's `JobMonitor`/webhook/`/api/jobs`/ETA layer, and checkpoint `prune`/CLI-`diff`/CLI-`branch`/Path-B-wrapper-git-bootstrap/snippets/gitgraph. `job-execution §7/§8` benchmark design is redundant with `benchmark-workflow.md` (a `job-system` source) — not separately migrated. **CODE FOLLOW-UPS surfaced by the doc-2 audit (queued for ops/closeout, not fixed mid-migration):** (a) stale `scheduler.gpu.mem` references in `runwrap.py:2821,3129-3132` + `bench/_cli.py:526` (the key is neither validated nor consumed); (b) the run decoder's `failed` state was unreachable — **RESOLVED (2026-07-27, user-directed mid-migration fix):** `_build_status` now maps the parser's `"error"` run-state to the envelope's `"failed"` state, the dead `END_MARKERS`/`FAILURE_MARKERS` constants are removed (the real markers live in the engine parsers), regression pinned by `test_status_failed_state_from_error_run`, and `running-a-job.md § 4.2` now describes the working four-state status; (c) stale `checkpoint.js:24` comment claiming "no graph viewer" (the lazy-loaded gitgraph viewer ships). The docs describe the *shipped* behaviour. **`job-system.md` done** (2026-07-27): consolidated `staged-execution` + `jobset-infrastructure` + `batch-workflow-overview` + `staged-relaxation-guide` + `slurm-integration` + `benchmark-workflow`, reconciled vs `jobset/*` + `siesta/stages.py` + `bench/*` + `runwrap.py` by two fresh-eyes digest agents. This doc **is** the current→target story: **CLI-shipped, web-pending** (verified absent: jobset blueprint, `/api/jobset/*`, `/api/checkpoint/branch`; Build tab is single-deck). Key reconciles: exactly **2 JobSet producers** (SIESTA ladder `stages_to_jobset` + bench sweep `sweep_to_jobset`) — the **PySCF ladder is an in-script loop, NOT a JobSet** (`cmd_pyscf` has no `--jobset`); bench is a producer but still *executes* inline-bash (D4 partial); `slurm-integration`'s "pre-implementation, no code exists" banner was stale (shipped single-node, §15-E pending); the JobSet "bundle" disambiguated from the handoff "bundle". Draft web work → `roadmap.md` workstream 1 (Phases 1-4, D7-gated). **`overview.md` composed LAST (2026-07-27)** — the execution start-here map + the current→target status matrix, verified vs `web/blueprints/` (web ships checkpoint routes + the Watch trajectory tab + run-wrapper install, but has **no** jobset wiring — confirming CLI-shipped/web-pending). **WAVE 5 COMPLETE** — 16 sources → 4 consolidated docs (`overview` + `job-contracts` + `running-a-job` + `job-system`), each fresh-eyes audited with every finding re-verified vs code; surfaced 3 code follow-ups — the unreachable decoder `failed` state (**fixed** 2026-07-27, see (b) above); stale `gpu.mem` comments + stale `checkpoint.js` graph-viewer comment (still queued for ops/closeout). **SECOND-PASS RE-REVIEW (2026-07-27, fresh-eyes agent, every finding verified vs code):** corrected 8 issues — (1) the multi-stage **suffix convention** was conflated: the ladder (`render_siesta_stage_fdfs` + stages runner + `stages_to_jobset`) names files `<label>_<stagename>` (underscore + name), while the `--stage N` overlay + the molwatch log + the decoder `_STAGE_RE` use `<label>-stage<N>` (hyphen + number) — `job-contracts § 2.3` rewritten to separate the two, resolving its contradiction with `job-system §5.2`; (2) `job-system §4.1` "`continue` retries inside its own `.run.sh`" was FALSE — `continue_retries` is honored only by the PySCF in-script ladder (`pyscf/input.py:868,929`), unimplemented for SIESTA where `continue`'s edge equals `halt`'s (`afterok`); (3) `job-contracts §2.4 step 3` "parse the `JOB` literal (`job_name`)" — the resolver greps `job_name = "…"` but scripts emit `JOB = "…"`; (4) `job-system §7` "five steps all under `molbuilder bench`" — `prep-bench`/`run-bench` are baked bundle executables, not CLI verbs; (5) `overview` SLURM cross-ref §5→§6; (6) the "named in code until it lands" companion notes upgraded to `?doc=` links now all four docs exist; (7) dropped opaque "(R5)" label; (8) added `--omp` to the wrapper flag table. **TWO NEW code follow-ups (queued for ops/closeout):** (d) SIESTA `continue_retries` is validated (`config/siesta.py:1304`) but consumed nowhere in `siesta/` — `continue` behaves like `halt`; (e) the PySCF dir-resolver regex greps `job_name = "…"` (`web/blueprints/watch.py`) which generated scripts never emit (they emit `JOB = "…"`), so discovery-chain step 3 is dead for current scripts (mitigated by steps 1/4). |
| 6 | `web/` (~33 sources / 845 KB — the biggest wave; **planned shape = 8 consolidated docs**: `molview` + `workspace` + `projects-sidebar` + `web-api` + `tabs` + `results` + `ui-contract` + `overview` composed LAST; the deferred `selection.md`→projects-sidebar merge lands here; `structure-guide.md`→a Modify/Build tabs doc; `molview-migration-plan.md`→roadmap/archive; img/ screenshots distribute with owning docs) | **DONE** (2026-07-27) — **`web/molview.md` done** (2026-07-27): the MolView family (6 sources → 1 consolidated doc) written from two verified fresh-eyes digest fact-sheets (dev-contract ~175 KB + user-guide ~38 KB of sources), every load-bearing claim reconciled vs `lib/molview/` + `lib/vibrationview/` + `lib/viewer/`. **Two surfaces in one doc** (E5): a plain-language user guide (§ 1–10) + the developer contract (§ 11–23) + the VibrationView sibling (§ 24), with a data-exchange diagram + runnable JS examples. Drifts corrected while writing: op `delete` (not `deleteAtoms`), the `calibrate` op added (8 total), `reloadFrames({forces})` not `{arrowsPerFrame}`, endpoints `/api/build/load` + `/api/modify/<op>` (verified), the 15-key `HANDLE_KEYS` carried verbatim, shape-glow selection (halos/2nd-model/dim-pop removed). Sources closed: `molview-module`, `molview-render-streamline`, `molview-esm-finalization`, `molviewer-guide`, `atom-selection-guide`, `vibrationview` (rows above). **Fresh-eyes audit of the composed doc DONE** (2026-07-27, every finding re-verified vs code): caught + fixed the `applyOp` table (`orient`/`add_atom` are role `anchor` not `subject`, and `reject` an empty selection with arity 2/1; `translate`/`rotate` act on `all` atoms when empty — added an `arity` column), the `§6` measurement examples (showed an impossible `O #0` — display is `idx+1` so `#0` never appears; → `#1`), and two over-tight "returns exactly {…}" statements. Noted stale code docstring: `measurements.js:34,40` carries the same `#0` example (cosmetic, not fixed mid-migration). ****`web/workspace.md` DONE** (2026-07-27) — the session-persistence module (2 sources: workspace-contract + workspace-guide), reconciled vs `lib/workspace/` + `state_timeline.py` by a fresh-eyes digest (every claim verified); written to the plain-language + diagram + worked-example standard (the user's *no-jargon* directive): two-copy persistence (sessionStorage mirror + server state timeline), the front-door/`snapshot-io` split, the MolView boundary (workspace = *where* the bytes go, not the data model / timeline policy), the mount-restore `hasRestorableSnapshot()` rule, and the save-flow-is-not-workspace note. Dead §4.6 working-copy pruned. save-flow.md deferred to the Modify/tabs doc. **Composed-doc audit DONE (clean, 2026-07-27)** — a fresh-eyes agent verified all load-bearing claims (every finding re-spot-checked): the 10-member public surface matches the `_PERSIST_SURFACE` guard test byte-for-byte, and the 30-step window + 300-file residue warning + the save-flow chain + the zero-data-accessors boundary all confirmed vs code; no findings. **`web/projects.md` DONE** (2026-07-27) — the sidebar file browser (3 sources: projects-sidebar + projects-sidebar-ui + selection cursor), reconciled vs `lib/projects/` (9 ES modules) by a fresh-eyes digest (doors + cursor + click-semantics re-spot-checked); plain-language + diagram + worked example + a runnable tab-author snippet. The two doors (`openMolecule`→`/api/build/load`, `saveMolecule`→`/api/structure/save`), the selection cursor, the UI pieces, the full public surface, and shipped-vs-planned. **Composed-doc audit DONE (2026-07-27)** — every load-bearing claim verified clean vs code (cursor, click-semantics, both doors, 409 handshake, all §5 methods+routes, the 1 MB preview threshold, the shipped graph viewer, the 9 canonical topics, and every §6 not-built item confirmed absent); fixed 2 findings (§7 named test globs that matched no files → real test names; §3 "file-only, never structure bytes" overreached — true for openMolecule, but saveMolecule posts the blob → reworded). **`web/presenters.md` DONE** (2026-07-27) — the Results-tab file-viewer registry (source: inspector-registry.md), reconciled vs `lib/inspectors/` by a fresh-eyes digest (registry surface + contract + `ctx` + core mount entries + ESM split re-spot-checked); plain-language, diagram + worked example, current→target ESM/rename note. Scope adopted (digest-advised): cores referenced not documented; Results-tab shell → separate `results.md`. **Composed-doc audit DONE (2026-07-27)** — near-clean: the 5-viewer table, contract, ctx, dispatch, thin-adapter, ESM split (2 of 9 ESM), 15 s poll, and registration-order example all verified vs code; fixed 1 imprecision (§2 said all four `ctx` helpers hit the projects file layer, but `showError` renders a local error card — only the 3 file helpers delegate). **`web/runtime.md` DONE** (2026-07-27) — the runtime registry + shared-primitives catalogue (3 sources: runtime-registry + runtime-registry-guide + notifications), reconciled vs `lib/molbuilder-runtime.js` (19-test pin) + the primitives by a fresh-eyes digest (surface + behaviour re-spot-checked); plain-language, sequence diagram + worked example, current→target ESM note. **Composed-doc audit DONE (2026-07-27)** — fixed 4 findings (notify has no auto-clear; markdownRender has 2 consumers not 3 — the file preview uses CodeMirror; the registry loads FIRST in <head> before 3Dmol which now loads last, not "after the 3D vendor script"; the example consumer is a module not a classic script); registry surface + behaviour + catalogue otherwise clean vs code. Scope adopted (digest-advised): **`form-schema.js` gets its OWN doc `form-schema.md`** (46 KB subsystem, not a primitive — new planned doc); region-label-* → transport doc; `xyz-io` → one-line pointer to model/structure (the one already-ESM primitive). Composed-doc audit: queued. **`web/form-schema.md` DONE** (2026-07-27) — the engine-option form builder (source: form-schema-guide.md), reconciled vs `lib/form-schema.js` + the server schema builder/routes by a fresh-eyes digest (surface + 9 field kinds + round-trip re-spot-checked); plain-language, dataclass→schema→form→values diagram, worked example, current→target ESM note. **Composed-doc audit DONE (2026-07-27)** — surface, the 9 field kinds (server↔JS match), the round-trip routes + `section`-gated exposure, and all three tab consumers verified clean; fixed 1 defect (the §3 call signatures dropped the required `schema` arg + reversed `renderForm` — corrected to `renderForm(host,schema)` / `collectForm(host,schema)` / `setValues(host,schema,values)`). **`web/web-api.md` DONE** (2026-07-27) — the server-routes contract (source: web-api.md, 80 KB), catalogue rebuilt from code (`blueprints/*.py`) by a fresh-eyes digest; conventions (`{ok,…}` envelope + `_shared.py` builders + status map + `_fetchEnvelope`), security posture (CSP/CSRF/rate-limit/cap), the complete grouped route catalogue cross-linking owner docs, full shapes for the un-owned groups, a round-trip, and the removed-routes list. **Composed-doc audit DONE (2026-07-27)** — ~40 routes spot-checked across every group (all exist with the stated method, no phantom routes), the envelope builders + status map + 50 MB cap + dead routes all verified clean; fixed 4 findings (scanner-sig examples were the digest's wrong `/wp-admin`/`.env` — real patterns are XSS/SQLi/path-traversal; `/api/modify/meta` is GET not POST; CSP quote had dropped `font-src`; the §3 grouping is by domain not by owning file). **`web/ui-contract.md` DONE** (2026-07-27) — the cross-cutting CSS/layout conventions (sources: ui-design-contract + mobile-layout fully; web-ui-coherence Rule 4 only), reconciled vs the actual CSS in `static/` by a fresh-eyes digest (the 5 layers + one-owner `.status` + tokens + breakpoints re-spot-checked). The sources' shipped-fixed debt narrative dropped — describes the CURRENT 5-layer model. Plain-language, layer diagram, the `.status`-looks-the-same example, the `[hidden]` gotcha, the CSP boundary. Composed-doc audit: queued. **`web/results.md` DONE** (2026-07-27) — the Results-tab dispatch shell (sources: tabs/results stub fully; results-tab + results-state-contract shell-halves), reconciled vs `results/viewer.js` + `lib/results/file-picker.js` + `/api/results/bundle` by a fresh-eyes digest (the `fileSelected`-event dispatch, the picker, the bundle, the `POLL_MS=15000` all re-spot-checked). Plain-language, dispatch diagram, worked example; points to presenters.md (registry) + trajectory/spectra (engines). **Final carve (digest-advised): 4 docs remain — `trajectory.md` (the engine), `spectra.md` (engine + standalone Spectra tab), `tabs.md` (structure-opt + transport + builder + the web-ui-coherence data rule), `overview.md` LAST.** Composed-doc audit: queued. **`web/trajectory.md` DONE** (2026-07-27) — the trajectory rendering engine (source: results-tab §§4.5–4.8 + results-state-contract trajectory half), reconciled vs `lib/trajectory/core.js` by a fresh-eyes digest (surface, the exact 4 plots, walltime tiers, poll/run_state re-spot-checked). Plain-language (data-feeder framing, the four plots, convergence targets, the walltime line, live polling with a diagram + worked example), documents the `error`/`errored` bug as a known gap. **Composed-doc audit DONE (2026-07-27)** — 11/12 claims verified clean vs `lib/trajectory/core.js`; fixed 1 real defect (§3 wrongly sourced the convergence-target lines from the run's *input* .fdf/.py — they come from the run's OUTPUT: the SIESTA `.out` `redata:` echo per `siesta.py:70-72`, or the sibling `.molwatch.log` header per `pyscf.py:555-566`) + 1 loose paraphrase (tier-1 walltime is an mtime-delta, not "file growth"); the `error`/`errored` known-gap re-verified exact on both sides (badge-Stopped uses a separate direct-read path — accurate). **`web/spectra.md` DONE** (2026-07-27) — the Raman spectra surface (sources: `tabs/spectra/spec.md` + the spectra half of `results-state-contract`), reconciled vs `lib/spectra/core.js` + `lib/inspectors/spectra.js` + `static/spectra/viewer.js` + `blueprints/spectra.py` by a fresh-eyes digest (two-surfaces-one-engine, chart axes, 2 s poll, VibrationView mount, render-vs-load, ESM split all re-spot-checked). Plain-language: two mounts of one engine (standalone `/spectrum-calculation` tab + Results-tab presenter), the chart (freq cm⁻¹ vs Raman activity Å⁴/amu · Lorentzian · red imaginary · density-mode), the mode table + excited-state panel, click-a-mode→VibrationView (distinct from the MolView inspect card), the standalone workflow, the render/load doors, 2 s live polling + in-memory-per-mount prefs, frozen-atom sidecar seeding, a two-mounts diagram + worked flow, and the current→target ESM note (engine + presenter still classic). Legacy-spec drift dropped + code follow-ups recorded (see the row). **Composed-doc audit DONE (2026-07-27)** — verified accurate + current vs code across all 12 claims; fixed 2 real defects (§3 mode table omitted the always-present "ES?" column — `_spectra_inspector.html:153`; §6 overstated "each load-error → its own HTTP status" — `_err_load` maps not_found→404/schema_mismatch→422 but malformed+field both →400). Everything else clean (axes, Lorentzian, density-mode, VibrationView-vs-MolView split, render/load doors, 2 s poll + in-memory-prefs TODO, ESM split, all tests, Mermaid + links). **`web/tabs.md` DONE** (2026-07-27) — the CONSUMER doc: how the six tab pages compose the reusable modules (sources: `tabs/molbuilder.md` + `tabs/structure-optimization.md` + `structure-guide.md` + `protocols/save-flow.md` + `protocols/web-ui-coherence.md` data half; transport read from code — no old_docs source), reconciled vs `app.py`/`tabs.py` + the four tab controllers + `build.py`/`transport.py`/`docs.py` by a fresh-eyes digest (tab roster/order, every mount + route, the three protocols, the ESM posture all spot-checked; the transport-is-functional reversal + the `?doc=` convention re-verified against code). Plain-language, one subsection per page (Molbuilder heaviest — holds the structure-generation in-gate; **Transport carries an explicit CURRENT-vs-planned status box** since its own `app.py`/`core.js` "placeholder/disabled" comments are STALE — Generate→`/api/transport/render`→transiesta `.fdf` works; no in-app save yet), the two cross-cutting protocols (save out-gate; the one data-coherence rule), an in-gate/out-gate framing, and the current→target ESM posture. Documents §5 highlights the `?doc=` link mechanism these migration docs depend on. **Composed-doc audit DONE (2026-07-27)** — verified accurate across the roster, the transport-works reversal, save flow, data-coherence, the `?doc=` mechanism, tests + links; fixed 2 real errors + 1 nuance (§3 SIESTA save writes the `.fdf` script not a `.xyz`, and step 4 rewrites the `psml_lib` form field not the on-disk `.fdf` — `viewer.js:1624`; §8 mis-listed `selection-bootstrap.js` as pure ESM — it's a hybrid publishing `molbuilderTab`, so NO tab controller is pure ESM yet; §2 clarified the sidebar load uses `openMolecule`, the shared door being `installMolecule`). **`web/overview.md` DONE** (2026-07-27, composed LAST) — the web map (source: `protocols/web-module-map.md`; also closes `results-tab-guide.md`): the **module-independence doctrine** (concealed / physically-separated / uniformly-reusable ES modules, with MolView as the exemplar — one module mounted read-only on 3 tabs + editable on 2), a **layers diagram** (tabs → modules → server), the **two universal patterns** (`{ok}` envelope + runtime registry), a **module registry** (every module → its doc + a VERIFIED ESM status: molview/vibrationview/workspace/projects/xyz-io full ESM; trajectory/transport hybrid; presenters/spectra/results/form-schema/runtime classic), the **ESM scorecard** + the #102/#103 follow-ups, and a "where to start reading" index. ESM statuses verified by grepping each module entry file for import/export; the 5-tab MolView mount-mode claim verified vs code. **Composed-doc audit DONE (2026-07-27)** — all 8 checks verified clean (the 5 mount modes, the full ESM registry, the two patterns, links, Mermaid safety, doc-mapping); fixed 2 real defects (§5 mis-assigned the `lib/spectra/core.js` ESM conversion to #103 — `roadmap.md § 3` bundles it + `trajectory/core.js` into the file-viewer/rename **#102** pass; and the "hybrid = publishes a global" footnote was wrong for `transport`, which self-mounts on `DOMContentLoaded` and publishes no global — `transport/core.js:716`). The same spectra-#103 drift was also fixed in `spectra.md § 9`. **★ WAVE 6 COMPLETE — all 13 web docs migrated + audited; every consumed source `_migrated_`. rate-limit.md correctly deferred to the ops wave (web-api.md § security points there).** **Cross-doc consistency review DONE (2026-07-27, 13999db)** — a whole-set sweep (the gap per-doc audits can't see) caught + fixed 4 real cross-doc issues, all verified vs code: presenters.md called `lib/trajectory/core.js`+`inspectors/structure.js` "already an ES module" while overview.md correctly calls them hybrid (reconciled to hybrid); trajectory.md + results.md were each missing the required current→target ESM note (added); an overview self-pointer "§6→§5"; a lone `# 24.` H1 in molview.md → `## 24.`. One agent "broken-anchor" finding (overview→molview §24) was a FALSE POSITIVE — §24 exists (two-surface doc: VibrationView at §10 user-guide + §24 dev-contract); not changed. Deferred (soft): molview/workspace call viewer instances "…inspector" (retired term) — blind rename risks real code identifiers (`#inspector-host`). **Module-inventory review DONE (2026-07-27, from-code enumeration)** — the user challenged whether modules were missed; a code sweep (10 `lib/` packages + 12 top-level files + all 22 `window.molbuilder.*` namespaces + every `register()`) confirmed VibrationView was the one genuinely-missed module (folded into molview §24) → **extracted to its own `web/vibrationview.md`** (14 web docs now); `lib/viewer/` is MolView's shared 3Dmol embed engine (contract = molview-module.md) NOT a peer, and the two atom models (`atomIndexModel`/`atomChannelModel`) verified correctly concealed within MolView (zero external readers; `atomIndexModel` exposed only via MolView's public re-export). **Architecture decision: full MolView/VibrationView separation → task #104** (see the `protocols/vibrationview.md` row). **Wave 7 (ops/) next.** |
| 7 | `ops/` (~5): install / deployment / rate-limit + config examples | pending |
| 8 | `process/` (~7): conventions, test strategy, audit playbook, CLI, package layout | pending |
| 9 | **The spine summary, composed last:** `design.md` (concise outline over the settled tree) + `architecture.md` (reuse map) + the `backend-architecture.md` companion decision | pending |
| 10 | Closeout: **cross-check `old_docs/` (all `_migrated_`) against the new tree**, then delete `old_docs/`, retire the freeze rule, archive the ledger, sweep memory pointers | pending |

`img/` assets distribute with their owning docs along the way.

## Ledger

One row per file in the frozen `old_docs/` tree. **Targets are
proposals** — the per-doc reconcile gate (README § Migration protocol)
confirms or changes them; record the outcome in Status + Notes.

Status values: `pending` | `moved` | `merged-into <doc>` | `archived`.

The old tree is FROZEN: `tests/test_docs_structure.py` fails if a file
appears in `old_docs/` that is not in this ledger.

| old_docs/ file | proposed target (docs/) | status | notes |
|---|---|---|---|
| `README.md` | README.md (merge: index absorbed) | pending | |
| `README_install.md` | ops/ | pending | |
| `architecture.md` | architecture.md | pending | **Composed LAST (Wave 9)** with `design.md` — the reuse map is a summary over the subsystems, so it settles after they migrate. `backend-architecture.md` kept SEPARATE (concern-lens companion), not merged. |
| `archive/2026-06-02-REVIEW_FINDINGS.md` | archive/ | moved | archived-history pass 2026-07-26 |
| `archive/2026-06-02-tabs-watch.md` | archive/ | moved | archived-history pass 2026-07-26 |
| `archive/2026-06-02-watch-api.md` | archive/ | moved | archived-history pass 2026-07-26 |
| `archive/2026-07-03-atom-selection.md` | archive/ | moved | archived-history pass 2026-07-26 |
| `archive/2026-07-03-embedded-viewer.md` | archive/ | moved | archived-history pass 2026-07-26 |
| `archive/2026-07-06-workspace-state.md` | archive/ | moved | archived-history pass 2026-07-26 |
| `archive/README.md` | archive/ | merged-into archive/README.md | supersession table carried over |
| `atom-selection-guide.md` | web/molview.md | merged-into web/molview.md | Wave 6 (2026-07-27), the user-guide selection behaviour (`§ 5`). Reconciled vs `lib/molview/` by a fresh-eyes digest agent (every claim verified): the mental model (one store; panel/viewer/measurements are consumers) holds, but every public global it named is renamed (store `molbuilder.molview.data.selection`, panel `molview.selection.mountPanel`, adapter `molview.selection.viewerAdapter`) and its "the adapter paints halos via setOverlays" claim is **stale** — the engine paints the amber shape-glow via `setSelectionHalo`; halos/second-model/dim-pop were removed. Carried the user behaviour, dropped the dev API prose. |
| `audit-2026-06-26/README.md` | archive/audits/ | moved | archived-history pass 2026-07-26 |
| `audit-2026-06-26/SYNTHESIS.md` | archive/audits/ | moved | archived-history pass 2026-07-26 |
| `audit-2026-06-26/T1_top_findings.md` | archive/audits/ | moved | archived-history pass 2026-07-26 |
| `audit-2026-06-26/T3_css_ui.md` | archive/audits/ | moved | archived-history pass 2026-07-26 |
| `audit-2026-06-26/T4_test_depth.md` | archive/audits/ | moved | archived-history pass 2026-07-26 |
| `audit-2026-06-27/README.md` | archive/audits/ | moved | archived-history pass 2026-07-26 |
| `batch-workflow-overview.md` | execution/job-system.md | merged-into execution/job-system.md | Wave 5 (2026-07-27). The big-picture batch overview (browser you-decide/molbuilder-does split, bundle contents, stage-sharing via symlinks, the run loop, monitoring via the same decoder) folded into `job-system.md` §§ 1, 4, 7; its honest CLI-built-vs-browser-pending status (§9, D7-D10) is the backbone of the doc's status matrix. |
| `checkpoints-guide.md` | execution/running-a-job.md | merged-into execution/running-a-job.md | Wave 5 (2026-07-27), `§ 6`. The plain-language checkpoint guide (git + `.binsnapshots` model, the three surfaces) folded in; reconciled vs `checkpoint.py` + `cli.py` snapshot group — **accurate, no drift** (all 8 CLI subcommands + 10 HTTP routes shipped as described). |
| `config.md` | execution/running-a-job.md | merged-into execution/running-a-job.md | Wave 5 (2026-07-27), `§ 5`. The `molbuilder.json` wrapper-contract + generator inputs reconciled vs `runtime_config.py`: discovery/merge order, `script_generation{preamble,activation}` + refuse-to-emit, `envs`. **Drift filled:** config.md predated the scheduler system and never documented `scheduler`/`execution`/`routing` — those are now first-class validated keys, added to `§ 5.3–5.4` with the `.sbatch` header sources (`render_sbatch`). Confirmed `scheduler.gpu.mem` is neither validated nor consumed (dropped). |
| `deployment.md` | ops/ | pending | |
| `design.md` | design.md (concise outline; §0 index → README, plans → roadmap) | pending | **Composed LAST (Wave 9)** as the concise map over the settled tree. §6 "Next steps" (tab-reorg = SHIPPED; transport B.3) already absorbed into `roadmap.md` 2026-07-26. The §0 index is retired (README.md owns it); design.md becomes a short outline that points at the detailed domain docs. |
| `engines/builders.md` | engines/builders.md | merged-into engines/builders.md | Wave 4 (2026-07-26). The structure-synthesis contract migrated + reconciled vs `peptide.py`/`nucleic.py`/`smiles.py`/`pubchem.py`/`residues.py`/`builders/backends/*`/`cli.py`. Fresh-eyes audit found **zero factual errors** and the doc *corrected* two stale source facts (H/heavy auto cutoff 0.3→**0.5**; the `add_hydrogens` tri-state + `relax_clashes` signatures the source omitted). Also fixed vs code: SMILES now RDKit-primary + **OpenBabel fallback** (parse: heme; embed: C60) + `return_backend`; clash thresholds `_CLASH_WARN_A=1.3`/`_CLASH_DANGER_A=1.0`; `_fix_methylene_hydrogens` now in `_amber.py:174`; `dispatch` gained `double_strand`/`strand2`; `auto_backend_name()`. Restored lost detail (why-not-`reduce`, the `_check_polymer_orientation` guard). Enriched: Vocabulary note + SMILES-fallback diagram + Python-API example + literal clash-warning + 5′/3′ table + tool references. Flagged a **code follow-up**: `build_peptide`'s docstring advertises `(SEP)`/dashed-3-letter notation the parser rejects (only `[SEP]` + single letters parse). H-placement mechanism deferred to `model/chemistry.md`. |
| `engines/optimization-tuning.md` | engines/tuning.md | merged-into engines/tuning.md | Wave 4 (2026-07-27). The cross-engine tuning guide migrated + reconciled vs `config/siesta.py`, `config/pyscf.py`, `siesta/input.py`, `pyscf/input.py`. Fresh-eyes audit found **every code-tied number correct**. Key reconcile: **SIESTA staged-opt has SHIPPED** (`SiestaStageSpec` + `_default_siesta_stages`; the source called it future `#542`) — documented the real ladder (stage1 CG/600/0.05/proceed, stage2 Broyden/200/0.04/halt, stage3 Broyden/100/0.01/halt off). Corrected: shipped globals differ from the tier guidance (`DM.Tolerance` 1e-5, `MeshCutoff` 300, `MaxSCFIterations` 500); the density-fit auto-pick is `def2-universal-jkfit` (hybrid → JK), not the source's `jfit`; `density_fit()` emitted bare (auxbasis None). Dropped internal task IDs (#534/#542). Enriched: extended Vocabulary, a stage-ladder diagram, `STAGES`/`.fdf` snippets, restored Verlet/Nose + GDIIS/RFO + Gaussian MaxStep + PBE0/M06-2X/r²SCAN refs. **CODE FOLLOW-UP:** 9 code sites referenced the old doc path `docs/engines/optimization-tuning.md` — repointed to `tuning.md` (companion commit). |
| `engines/pyscf-publication-guide.md` | engines/pyscf.md (merged) | merged-into engines/pyscf.md | Wave 4 (2026-07-26). Publication-quality params (basis/functional tiers, D3(BJ) spelling caveat, wb97x-d blacklist), the methods-section template + citations (E4 preserved), and the in-script staged-opt design folded into `pyscf.md` §§ 5–7. Reconciled vs code — **fixed source drift**: current default is B3LYP-D3BJ/def2-SVP (dispersion ON, not "no dispersion"); shipped stage-3 default `gmax=2.0e-4` (source said 1.5e-5). The cross-engine tier framework stays a pointer to `tuning.md`. |
| `engines/pyscf.md` | engines/pyscf.md | merged-into engines/pyscf.md | Wave 4 (2026-07-26). The `.py` script emitter contract migrated + reconciled vs `pyscf/input.py` + `config/pyscf.py` + `validation/pyscf.py`. `pyscf-publication-guide.md` merged in (params/methods-template). Line numbers verified (`render_script:131`, `convert:1291`, `_emit_stages_loop:800`, `StageSpec:59`, `_default_stages:173`, presets `:314`). |
| `engines/siesta-gpu.md` | **SPLIT** — GPU setting/perf → engines/siesta.md § 7.1; build/env recipe → ops/ | pending | Wave 4 (2026-07-26): decided GPU is *just a different eigensolver setting*, so the setting/perf half (ELPA/CUDA options, MPS rank policy 4-with / 2-without, numerical equivalence ~1e-6 eV, and "what to look for" incl. the silent-CPU-fallback `elpa gpu codepath` canary) folded into `siesta.md § 7.1`. The build/deployment recipe (conda env, CMake flags, `$ORIGIN` rpath, sentinel-resume, toolchain fingerprint, env-var overrides, MPS-daemon internals — `envs/recipes.py`/`builds.py`) is a deployment concern → **ops/ (Wave 7)**. Source stays unprefixed until the ops half lands there. |
| `engines/siesta.md` | engines/siesta.md | merged-into engines/siesta.md | Wave 4 (2026-07-26). The `.fdf` emitter contract migrated in full + reconciled vs `siesta/input.py`, `config/siesta.py`, `makov_payne.py`, `memory.py`, `stages.py`. Line numbers verified (`render_fdf:329`, `convert:1486`, `SiestaConfig:114`, `SiestaStageSpec:1235`, presets `:1414`, …). Added E5 two-surface framing + Mermaid + worked examples; mapped the two sibling modules the source omitted (`makov_payne` post-process, `memory` estimator → pointers). Cross-refs: the GPU/ELPA *setting + performance* now owned by § 7.1 here (the `siesta-gpu` split, 2026-07-26); the GPU/ELPA *build/env recipe* → ops/ (Wave 7); stage-ladder *scheduler* execution → execution/; SIESTA half of `optimization-tuning` → `engines/tuning.md` (this wave). |
| `engines/transport.md` | engines/transport.md | merged-into engines/transport.md | Wave 4 (2026-07-26). The stub ("will own the end-to-end contract when it grows") is now the real doc — the four transport sources consolidated + reconciled vs `transport/*.py` + `config/transport.py`. Modern `TS.Elec`/`TS.ChemPots` emit (`transiesta.py:487`), `_find_electrode_regions:195`, `TransportConfig:119`, and the invariant-set gate ids verified against `preflight.py`. |
| `examples/molbuilder.asu-sol.json` | ops/examples/ | pending | |
| `form-schema-guide.md` | web/form-schema.md | merged-into web/form-schema.md | Wave 6 (2026-07-27). Reconciled vs `lib/form-schema.js` + the server `dataclass_to_form_schema`/`_field_to_schema` (`_shared.py:488,609`) + the routes (`/api/build/schema/<engine>` `build.py:1000`, `/api/transport/schema` `transport.py:51`) by a fresh-eyes digest, load-bearing facts re-spot-checked: the 4-method surface (`fetchSchema`/`renderForm`/`collectForm`/`setValues`, plain global — no `register`), the **9 field kinds** (server↔JS match exactly, confirmed by grep), the dataclass-is-source-of-truth round-trip (only `section`-tagged fields exposed). **Drift corrected vs the frozen source:** the guide wrongly said `fetchSchema` covers transport (transport fetches its OWN `/api/transport/schema`, `fetchSchema` would 404) and omitted the `comma-floats` kind. **CODE FOLLOW-UPS (stale comments, queued):** `form-schema.js:28-45` + `_shared.py:520` docstrings omit `comma-floats`/`stage-table`; `molbuilder-runtime.js:36` lists `formSchema` as a registered module but it never registers (plain global). |
| `img/SCREENSHOTS.md` | (distribute with owning docs) | pending | |
| `img/hero-molbuilder.png` | (distribute with owning docs) | pending | |
| `img/molbuilder-workspace.png` | (distribute with owning docs) | pending | |
| `img/results-bundle-card.png` | (distribute with owning docs) | pending | |
| `img/results-spectra.png` | (distribute with owning docs) | pending | |
| `img/results-trajectory.png` | (distribute with owning docs) | pending | |
| `img/sidebar-projects.png` | (distribute with owning docs) | pending | |
| `img/spectrum-form.png` | (distribute with owning docs) | pending | |
| `img/structure-optimization-form.png` | (distribute with owning docs) | pending | |
| `img/tab-bar.png` | (distribute with owning docs) | pending | |
| `img/transport-form.png` | (distribute with owning docs) | pending | |
| `job-case-analysis/ANALYSIS-G1K1C4.md` | archive/audits/ | moved | archived-history pass 2026-07-26 |
| `job-execution.md` | execution/running-a-job.md | merged-into execution/running-a-job.md | Wave 5 (2026-07-27). The single-job front-door: its plain-language big picture, the end-to-end workflow, and the **standalone/detection (T/M/C/A/H) contract** → `running-a-job.md §§ 1–2`; the wrapper's runtime mechanics (pulled from `runwrap.py`, which the old doc only delegated to) → `§§ 3–4`; the config templates → `§ 5`. Its §7/§8 **benchmark-design** sections (`mbbench`→shims, `bench-manifest@1`→`@2`, the `(G,K,c)` grid, run-bench matrix, domains, submit/direct) are **superseded and redundant** with `benchmark-workflow.md` — deferred to `job-system.md`, not separately carried. Stale `scheduler.gpu.mem` template line dropped. |
| `jobset-infrastructure.md` | execution/job-system.md | merged-into execution/job-system.md | Wave 5 (2026-07-27). The plain-language JobSet explainer (producers→JobSet→engines, ladder + sweep use cases, ops cheat-sheet) folded into `job-system.md` §§ 1-4; reconciled vs code (all CLI verbs + `fdf --jobset` exist as written). The transport diamond-DAG use case (§7) is a named-but-unbuilt extension (`depends_on` is single-parent) — noted in § 2 + § 7. |
| `molbuilder.json.example` | ops/examples/ | pending | |
| `molviewer-guide.md` | web/molview.md | merged-into web/molview.md | Wave 6 (2026-07-27). Mined for embed-contract facts only: it documents the low-level `viewer.embed` handle, which is now the **internal, concealed** component — the public door is `molview.mount(host, workspace, {mode, owner})`, and tabs no longer call `viewer.embed` (only VibrationView borrows it). Verified the embed handle methods still exist; the guide's "the component every tab drops in / selection = setOverlays" framing is superseded (selection is `setSelectionHalo` glow). Its content became `web/molview.md § 11–13` (the door + `mount` + handle). |
| `package-layout.md` | process/ | pending | |
| `protocols/archive/2026-07-05-browser-data-contract.md` | archive/ | moved | archived-history pass 2026-07-26 |
| `protocols/archive/2026-07-05-working-copy-persistence.md` | archive/ | moved | archived-history pass 2026-07-26 |
| `protocols/archive/2026-07-06-molview-module.md` | archive/ | moved | archived-history pass 2026-07-26 |
| `protocols/atom-annotations.md` | model/structure-annotations.md | merged-into model/structure-annotations.md | Wave 2 (2026-07-26), sub-doc of structure.md. The per-atom channel model. Verified vs code — corrected: sidecar is **schema v6** now (annotations added at v4); JS L1 moved `lib/workspace/` → `lib/molview/_atom-channels.js`. "IN PROGRESS phases" resolved: model + persistence + block-recovery + built-in translations SHIPPED; open (value-channel producer + `by_value`, generic fdf-strategy) → roadmap. Stale `§7` reference dropped. |
| `protocols/backend-architecture.md` | (root, companion to architecture.md) or merge into it -- decide at reconcile | pending | |
| `protocols/benchmark-workflow.md` | execution/job-system.md | merged-into execution/job-system.md | Wave 5 (2026-07-27), `§ 6`. The target-isolated 5-step benchmark workflow (generate→prep-bench→run-bench→summarize→prep-run) reconciled vs `bench/*` — the source §0 status ("all built") is accurate. Migrated: `environment@1`/`bench-manifest@2`/`bench-result@1`, the `.fdf` surgery (isolate hardware-not-solver), the `(G,K,c)` grid, winner=fastest s/iter + `recommend_resources`, `probe-scheduler`. **Drift fixed:** §8.1 workstation rows said `MOLBUILDER_MPI_NP`/`MOLBUILDER_OMP_NUM_THREADS` but the adapter emits `MB_NP`/`OMP_NUM_THREADS` (the `MOLBUILDER_*` bug was fixed 2026-06-28); manifest `@1`→`@2`. |
| `protocols/bundle-contract.md` | execution/job-contracts.md § 5 | merged-into execution/job-contracts.md | Wave 5 (2026-07-27). The run→next-stage handoff object folded into `job-contracts.md § 5` as **"the workflow handoff bundle"** rather than a standalone `handoff-bundle.md` (user's *fewer-docs* directive) — R5 still honoured: no file is named bare "bundle", the section uses "handoff bundle" vocabulary, and "bundle" stays reserved for JobSet. **Heavily reconciled — the API names had drifted the most of any source:** `RunBundle`→**`BundleResult`** (`parse/types.py`), `ScriptSource`→**`ScriptResult`** (`user_custom_lines`→`user_custom`), free `assemble_from_run_dir`→**`BundleDirParser.parse`** (private `_assemble_from_run_dir`→dict), writer raises **`BundleWriteError`** not `BundleError` (two distinct exceptions), schema **v3+v4** ("expects 4"), handedness checks moved to `parse/dirs/_assembler_helpers.py`. |
| `protocols/chemistry-correctness.md` | science/chemistry-correctness.md | merged-into science/chemistry-correctness.md | Wave 3 (2026-07-26). The control-surface map (5 points) + audit checklist migrated in full, ASCII flow → Mermaid; the `(charge, spin)` science from `science.md §2` merged in (conventions, coordination-dependent spin, primitives catalog, hemeC-dithiol post-mortem, cross-engine rule, auto-detect guard). Line numbers/test names re-verified vs code — corrected the source's stale ones: `_fix_methylene_hydrogens` :160→:174; `TestAddHydrogensIdempotent` (gone) → real `maybe_add_hydrogens` tests in `test_nucleic.py`; `test_build_fdf_runs_preflight_before_render` (gone) → `test_preflight_returns_issues_for_{siesta,pyscf}` (`test_web.py:435,459`). |
| `protocols/cli.md` | process/ | pending | |
| `protocols/code-audit.md` | process/ | pending | |
| `protocols/code-conventions.md` | process/ | pending | |
| `protocols/data-vocabulary.md` | **SPLIT** — atom-index → model/overview.md; §5 → structure.md; §1–3 → execution/job-contracts.md | merged-into (split) | Wave 5 (2026-07-27) closes the split: §1 (persisted-artifacts registry), §2 (config↔scheduler parameter vocabulary), §3-intro (identifier/path conventions) → `job-contracts.md § 6`, reconciled vs `persist.py`/`bench/*`/`jobset/model.py`/`checkpoint.py`/`runtime_config.py`/`siesta/stages.py` — all 9 schema strings + the whole §2 translation table verified row-for-row; `check_schema_major` real + consolidated to its 3 adopters. Corrected: the "decoded run" is **served, not a `decoded.json` file** (`decode_run_dir`→`JobResult`); the ladder per-job SLURM `-J` is the **bare stage name** (`stage1`), not `job-stage<N>`; `.molstruct.json` also uses a bare-int schema (=6). Earlier halves (Wave 2): §3.1/§3.2 atom-index → `model/overview.md`; §5 structure-metadata → `structure.md § 2.2`. Fully consumed. |
| `protocols/frontend-module-architecture.md` | web/ | pending | |
| `protocols/inspector-registry.md` | web/presenters.md | merged-into web/presenters.md | Wave 6 (2026-07-27). Reconciled vs `lib/inspectors/*.js` + `results/viewer.js` by a fresh-eyes digest (registry surface, the contract, the `ctx` members, the core mount entries, and the ESM/classic split all re-spot-checked). **Titled `presenters`** with a current→target note (code is still classic `molbuilder.inspectors`; rename+ESM = task #102). SCOPE (digest-advised, adopted): the heavy `lib/trajectory/core.js` + `lib/spectra/core.js` engines are REFERENCED not documented (their internals → the Results/Spectra tab docs); the Results-tab shell (`lib/results/` file-picker + bundle-handoff, `results-tab`/`results-state-contract`) → a separate `results.md` (those rows stay pending); only the `isResult`/`resultCategory`/`pickResult` CONTRACT fields stay here. **Drift corrected vs the frozen source:** it documented only 4 inspectors (markdown is a 5th), a stale `ctx` (only `readFile`; shipped has `showError`/`readFile`/`readRange`/`writeFile`), and wrongly claimed the Documents tab uses the registry (it renders markdown via `markdown-render.js` directly). **CODE FOLLOW-UPS (queued):** the markdown inspector is absent from `INSPECTORS`/`EXPECTED_ORDER` in `test_results_blueprint.py` + the node dispatch mirror — the `.md` markdown-beats-source dispatch is UNPINNED (test gap); `registry.js:86` docstring omits `pickResult`/`_clear`. |
| `protocols/job-decoder.md` | execution/running-a-job.md | merged-into execution/running-a-job.md | Wave 5 (2026-07-27), `§ 4.2`. The source was a "v1 draft"; reconciled vs `parse/dirs/job.py`. **Only the shipped half migrated** — the directory decoder `decode_run_dir` → in-memory `JobResult` (SIESTA-only; `job_type` classification, the running/stale/finished/failed status machine, the 25-key `engine_body_summary`, per-stage plot buckets, progress counters), consumed today by the web Results tab + `jobset/runstatus.py`. The **entire draft layer above it → `roadmap.md` (R3)**: `JobMonitor` thread, `/api/jobs/{id}/decoded` + `decode-once` endpoints, the trigger/event model, webhook delivery + `webhook_log.jsonl`, and the ETA/per-iter timing (`estimated_remaining_s` is a Phase-1 `None` stub). Note: the decoded run is served, not a `decoded.json` file. |
| `protocols/job-layout.md` | execution/job-contracts.md | merged-into execution/job-contracts.md | Wave 5 (2026-07-27), § 2. The directory + filename protocol reconciled vs `projects.py`/`runwrap.py`/`web/blueprints/watch.py`/`trajectory_log/format.py`. Corrected: **nine** canonical topics not six; stdout is run-indexed `-runN.out`/`-runN.pyscf.log`; activation is a configurable `conda activate` line not `conda run`; ELPA/GPU `.fdf` re-routes to `molbuilder-siesta-gpu`; `find_geom_candidates` globs are `*_optimized.xyz`/`*.STRUCT_OUT`/`*_geom_optim.xyz` (no bare `*.xyz`/`*.pdb`); the discovery chain + multi-log merge verified exact. |
| `protocols/mobile-layout.md` | web/ui-contract.md | merged-into web/ui-contract.md | Wave 6 (2026-07-27). Accurate + current vs code; its `min()`-floor grid rule + the `.card-row` primitive folded into `web/ui-contract.md § 3` (verified: `form-schema.css:156`, `page-shell.css:371`). |
| `protocols/molview-esm-finalization.md` | web/molview.md | merged-into web/molview.md | Wave 6 (2026-07-27). The plan tail (Phase B-internal / C / D) + consumer inventory were already absorbed into `roadmap.md` §3 (2026-07-26) — migration bookkeeping, not contract. The **durable** fact — the current ESM status (consumers all `type="module"` + import the door; Phases A/B done; the transitional `molbuilder.molview.*` publishes are KEPT live seams, not dead scaffolding) — landed in `web/molview.md § 12`, verified vs `lib/molview/index.js`. |
| `protocols/molview-migration-plan.md` | roadmap.md (merge; archive rest) | pending | Open-work tail (D3-tail / D4 / A3 / A4 / Step 6) absorbed into `roadmap.md` §3 2026-07-26; the shipped-orientation + anti-drift scaffolding archives when this row closes (Wave 3). |
| `protocols/molview-module.md` | web/molview.md | merged-into web/molview.md | Wave 6 (2026-07-27), the **developer-contract backbone** (~120 KB source, ~95% code-accurate). Reconciled vs `lib/molview/` by a fresh-eyes digest agent (every load-bearing claim spot-checked): the data model + `mount` + the 15-key `HANDLE_KEYS` (pinned by `tests/test_molview_mount_js.py`) + the four-tier engine + the timeline + the view sub-namespace all carried. Drifts corrected in the new doc: the op name is **`delete`** not `deleteAtoms`, the op registry gained **`calibrate`** (8 ops total), `reloadFrames(frames, {forces})` not `{arrowsPerFrame}` (dead legacy), `applyOp` posts to `/api/modify/<op>` + `installMolecule` to `/api/build/load` (verified endpoints). Wire field-shapes (`§ 21`) cross-referenced to `web-api.md` rather than duplicated. |
| `protocols/molview-render-streamline.md` | web/molview.md | merged-into web/molview.md | Wave 6 (2026-07-27). Folded into the render-engine section (`web/molview.md § 16, 19`) rather than kept separate — heavy overlap with molview-module § 14. The four update tiers, the `StructureData`/`ViewFlags`/`ProcessedFrame` shapes, and the shape-glow selection (`_SELECTION_GLOW = {#ffd54a, 0.7, 0.5}`) all verified vs `lib/molview/engine/`. Its § 8.1 steps 2–4 (finer-grained invalidation) are TARGET, not shipped → flagged as planned (roadmap), not carried as contract. |
| `protocols/notifications.md` | web/notifications.md | moved → web/notifications.md | Wave 6 (2026-07-27). Initially folded into `runtime.md § 3`; **extracted to its own `web/notifications.md`** in the post-Wave-6 shared-primitive review (user: notify is a first-class notification framework; the code names `notifications.md` as its contract). Verified vs `app-notifications.js`: the general API (`show`/`clear`/`clearAll`/`list`, error/warn/info, dedup-by-id + ×N, runtime-registered), the display (host+CSS+JS co-located in `_app_header.html` → no silent-drop; a11y `role=alert`/`status`+`aria-live`), and the one built-in source (`molbuilder:persist-error` from `workspace/dispatcher.js`). **DESIGN VERDICT (from code): well-built as a general framework, display correct.** **ESM conversion + build-out = task #105** (own doc + auto-dismiss `ttl` for transient messages — today nothing auto-dismisses — + decide the unused `detail`). **Composed-doc audit DONE (2026-07-27, CLEAN)** — all 7 claim groups + links verified TRUE vs `app-notifications.js` + `_app_header.html` (host/CSS/JS co-located, all 6 tabs include it) + `dispatcher.js` (the event); no doc fixes. **CODE FOLLOW-UP (queued, fold into #105):** `app-notifications.js:1`, `_app_header.html:65`, and `test_app_notifications_e2e.py:1` still cite the pre-migration path `docs/protocols/notifications.md`, and two in-code a11y comments cite "§5" (a11y is `§2` in the new doc) — stale references in code/tests, harmless. |
| `protocols/parse-module.md` | model/parse.md | merged-into model/parse.md | Wave 2 (2026-07-26). The shipped parse-stack contract (3 ABCs, ParseResult hierarchy, registry, layout, plugin/composer/forbidden-pattern rules). Verified vs `parse/` — corrected the stale §5 layout: the "PENDING" coords parsers (`siesta_fdf.py`, `xyz.py`) were never added; added the real helper modules. The huge §8 migration plan (Phases A–H, all shipped 2026-06-21) → one History note (R3). |
| `protocols/playwright-tests.md` | process/ | pending | |
| `protocols/projects-sidebar-ui.md` | web/projects.md | merged-into web/projects.md | Wave 6 (2026-07-27), the UI half — all verified accurate vs code (breadcrumb, entry list + `⋯` kebab's four menu shapes, the file-type filter, the mutation bar's three buttons, the dialog factory, the preview pop-up's size thresholds + mtime-safe save, layout/resize/collapse/drawer, the lock banner). Folded into `web/projects.md § 4`. The `[hidden]` visibility-trap CSS convention is noted; the `.is-hidden`/ban-`hidden=` migration (master M7) is still a known gap. |
| `protocols/projects-sidebar.md` | web/projects.md | merged-into web/projects.md | Wave 6 (2026-07-27), the SPINE. Reconciled vs `lib/projects/*.js` (9 ES modules) + the `/api/files/*` + `/api/build/load` + `/api/structure/save` routes by a fresh-eyes digest (load-bearing claims re-spot-checked: the two doors, the `current_dir`/`current_file` cursor, single-click=`setShared`/double-click=`publishCommit`, `onChange` fires-once-on-register vs `onCommit` not). Trimmed the design-vs-code hedging (the code has SHIPPED most of §0.1). The §13 migration ledger folded in as **shipped-vs-planned**: M1–M5/M8/M10 shipped; M6 (cross-tab storage listener — grep-confirmed NOT wired), M7 (`.is-hidden`), M9, M11–M15, and §15.5 (per-file mtime column / sort / `.md`→Documents dispatch) surfaced as known gaps, NOT presented as shipped. **CODE FOLLOW-UPS (stale comments, queued):** `api.js:87` `apiRoots` comment says roots returns "no top-level ok" but the server now returns `{ok:true}` (`files.py:305`); `preview.js:77` `EDIT_MAX_BYTES=32 MB` is vestigial (the real editability gate is `VIEW_ONLY_BYTES=1 MB`); `checkpoint.js:24` "no graph viewer" comment (the gitgraph viewer ships — the same known follow-up as the execution wave). |
| `protocols/pseudopotential-validation.md` | science/pseudopotentials.md | merged-into science/pseudopotentials.md | Wave 3 (2026-07-26). The full `.psml` check contract (C1–C6, severity model, "what is NOT checked", process, conformance map, KB/ONCVPSP/PseudoDojo refs) migrated + reconciled vs `pseudos.py`/`validation/siesta.py`/`cli.py`. **Reconciled a real drift:** the source's single-WARN `xc_mismatch` (with a "should family mismatch be ERROR?" reviewer note) is now the implemented split — `xc_family_mismatch`→ERROR (GGA/LDA), `xc_mismatch`→WARN (author). Line numbers verified; ASCII → Mermaid entry-point diagram added. |
| `protocols/rate-limit.md` | ops/ | pending | |
| `protocols/region-labels.md` | **SPLIT** — vocab → model/structure-annotations.md; emitter/bias/refs → engines/transport.md | merged-into (split) | Vocabulary (the `-electrode` convention, canonical `L`/`R`/`bridge`/`interface`, `is_electrode_label`) → `structure-annotations.md § 5` (Wave 2). **Transport half now landed (Wave 4, 2026-07-26):** `_find_electrode_regions` z-sort → Left/Right chempot, `%block TS.Elec.<name>` emit, atom-ordering-by-position, bias direction, the Brandbyge/Stokbro/Reed/Solomon references → `engines/transport.md` §§ 4, 9. Fully consumed; prefixed `_migrated_`. |
| `protocols/results-state-contract.md` | web/ (SPLIT: results.md + trajectory.md + spectra.md) | merged-into web/{results,trajectory,spectra}.md | Wave 6 (2026-07-27): the durable core (the 5-state machine IDLE→LOADING→LOADED/WATCHING→ERROR, the 5 state buckets, "Refresh = file-switch with same path", the file-identity + in-progress-frame invariants) is distilled — the one-paragraph summary → `web/results.md § 4`; the per-engine state map → `trajectory.md` (15 s poll, 2-tick finish buffer) and `spectra.md` (2 s poll, single sticky `allPhasesComplete`, in-memory-per-mount prefs). Dropped the 80% design-proposal + per-PR ledger + parser line-citations. **Fully consumed once all three engine/shell docs landed (2026-07-27).** **CODE FOLLOW-UP (queued, VERIFIED — the WEB-layer instance of the known `run_state` drift):** `trajectory/core.js:480` gates the ERROR lifecycle on `run_state === "errored"`, but the backend emits `"error"` (never `"errored"` — `_helpers.py:181`, `molwatch.py:144`, `siesta.py:901/912/948`), so the ERROR transition is unreachable and a crashed run keeps polling every 15 s (the badge shows Stopped via a separate direct-read path, so it looks right); one-line fix `"errored"`→`"error"`. Documented as a known gap in `trajectory.md § 4`. (Spectra's state contract has no such drift — it keys on `phase_*` markers, not `run_state`.) |
| `protocols/results-tab.md` | web/ (SPLIT: results.md + trajectory.md) | merged-into web/results.md + web/trajectory.md | Wave 6 (2026-07-27). §§1–2.2 (dispatch shell + file→viewer table) → `web/results.md`; §§4.5–4.8 (the 4 plots / dual-trace force / convergence targets / SCF walltime tiers / CSV) → `web/trajectory.md` — both reconciled vs `results/viewer.js` + `lib/trajectory/core.js` (the exact 4-plot set, the walltime tiers, `POLL_MS=15000` re-spot-checked). §§3–4 (build-plan) dropped as historical. **DEAD source content dropped:** §2.6 sidebar-`onChange` dispatch (retired #301), §3.2/§2.5 the `/watch` page + `static/watch/viewer.js` (retired 2026-05-19, dir gone), §2.7 Markdown→DOCUMENT tab ([PLANNED]). **DRIFT corrected:** "60 s poll" → `POLL_MS=15000` (15 s); CSV `# source path` is REDACTED not raw. **CODE FOLLOW-UP (real bug, queued):** `trajectory/core.js:480` checks `run_state==="errored"` but the backend emits `"error"` (`_helpers.py:181`) — the ERROR state is unreachable, so a crashed run polls forever (badge shows "Stopped" correctly but the timer never stops); one-line fix `"errored"→"error"`. Same class as the fixed decoder `failed` gap. |
| `protocols/run-checkpoints.md` | execution/running-a-job.md | merged-into execution/running-a-job.md | Wave 5 (2026-07-27), `§ 6`. The checkpoint contract (the `.mbcheckpoint.json` config, `.binsnapshots/<sha>/MANIFEST` 3-col dedup, verify-before-mutate restore, engine archive_globs) reconciled vs `checkpoint.py`. **Draft-not-shipped extracted to `roadmap.md` (R3):** `prune_archives`, `list_branches`/`list_tags`, the `snapshot diff` + `snapshot branch` *CLI* faces (Python/HTTP exist), the wrapper-auto-git-bootstrap "Path B" + `echo_git_status` (the wrapper is deliberately git-agnostic), the `molbuilder/snippets/` git-snippet library, and the `@gitgraph/js` commit-graph viewer. Shipped-vs-design dataclass trimming noted. |
| `protocols/runtime-registry.md` | web/runtime.md | merged-into web/runtime.md | Wave 6 (2026-07-27), the registry contract. Reconciled vs `lib/molbuilder-runtime.js` (19-test pin) by a fresh-eyes digest + re-spot-checked: the 5-method surface (`register`/`whenReady`/`get`/`listRegistered`/`listPending`), `register` throws-on-bad-input / warns-and-replaces on re-register, `whenReady` **rejects (never throws)** on a bad name and works before-register, microtask error-isolation, load-first rule. The source's §4 registered-module NAME table is **stale** (lists `modify.handle`; predates `molview`/`selection.store`/`structure.*`) — dropped; the new doc describes the naming *scheme* + `listRegistered()` instead of pinning names. **CODE FOLLOW-UP (stale comment, queued):** `molbuilder-runtime.js:32-45` header still lists `style`/`fmt`/`modify.handle` as registered when they aren't (they became global-less ES modules). |
| `protocols/save-flow.md` | web/tabs.md | merged-into web/tabs.md | Wave 6 (2026-07-27). The page-level Save UX → `tabs.md § 6` ("the out-gate"), reconciled vs `modify/structure/save.js` + `save-dialog.js` + `build.py:634-674` by a fresh-eyes digest (dialog → sidebar-current-dir → `saveMolecule(path,{overwrite})` → `POST /api/structure/save`; server writes the `.xyz`+`.molstruct.json` pair via `StructureCodec.write`; viewer-is-truth `exportFile`; 409 `needsOverwrite` handshake all re-verified). **The source's drifted write-path spec** (two `projects.writeFile` calls + "no single-endpoint POST") was corrected to the shipped ONE-POST/server-writes-the-pair path (task #75). `web/workspace.md § 8` keeps only the boundary note (save is not the workspace). |
| `protocols/scientific-validation.md` | science/validation.md | merged-into science/validation.md | Wave 3 (2026-07-26). The runtime validation machinery: the L1→L4 layering, `analyze_structure`→`ChemistryAnalysis`, the 3 noble-metal categories (with the TranSIESTA/NEGF literature preserved, E4), the adapter registry, the consumers (`check_open_shell_metal` + `/api/structure/analyze`), Pattern-B notice, adding-an-engine, the `validation/` package layout, test invariants. Line numbers re-verified vs `chemistry.py`/`validation/`/`auto_defaults.py`. **SPLIT:** §7.5 GPU eigensolver (ELPA-CUDA / NVIDIA-MPS / `molbuilder-siesta-gpu` env) — the setting/performance/numerical-equivalence half folded into `engines/siesta.md § 7.1` (Wave 4); the env-build / MPS-daemon internals → ops/ (Wave 7). A pointer note stands in its place. |
| `protocols/script-contract.md` | execution/job-contracts.md | merged-into execution/job-contracts.md | Wave 5 (2026-07-27), § 3. The reserved comment-block grammar reconciled vs `script_emit.py`/`siesta/input.py`/`pyscf/input.py`/`transiesta.py`. Corrected to code: ATOM-METADATA is **v4** (`molstruct-json/v4`, added the `annotations` trigger; sidecar file v6), not v3; BENCH-MARKS gained `Diag.Algorithm type=enum` (type set now `{int,float,str,pow2,enum}`) and is **SIESTA-`.fdf`-only** (PySCF bench still a gap); **HEADER is reserved but not emitted** (`emit_header` defined, zero call sites); added the verified per-engine **emit matrix**. |
| `protocols/script-execution.md` | execution/job-contracts.md | merged-into execution/job-contracts.md | Wave 5 (2026-07-27), § 4. The warm/cold-restart contract reconciled vs `runwrap.py`/`pyscf/input.py` — **essentially all shipped and accurate** (the most reliable of the five sources). Verified: the four behaviours, SIESTA 13-suffix + PySCF 5-file `--cold` glob inventories, the PySCF `chkfile` + `_atom_block` warm-restart blocks, project-ID `awk` extraction + sanitizer, all five banner modes, `#539`/`#534` both closed. Minor flag-label drift fixed (`--continue`=`-c`, `--force`=`-f`). |
| `protocols/selection.md` | web/projects.md | merged-into web/projects.md | Wave 6 (2026-07-27). Its cursor SEMANTICS (the two `current_dir`/`current_file` slots; single-click=preview→`onChange`, double-click=commit→`onCommit`; the canonical topics; the project-bootstrap tree) merged into `web/projects.md §§ 2–3`; its **API list was DROPPED as a stale 7-method snapshot** — it omits `onCommit`/`publishCommit`/`setShared`/`navigateTo` + the whole mutator/lock surface + `parser.js`/`checkpoint.js`, superseded by the master's §0.1 (the reconciled surface is in `projects.md § 5`). Its self-referential "files.py 501-stub header is stale" note is itself now stale (`files.py:953` corrected) — dropped. |
| `protocols/sidecar-contract.md` | **SPLIT** — §11–12 → model/structure-molstruct.md; §1–10 → engines/overview.md | merged-into (split) | §11 (rename/move/copy pairing) + §12 (consumers) → `structure-molstruct.md` (Wave 2). **§1–10 now landed in `engines/overview.md` § 3 (Wave 4, 2026-07-27):** the 3-stage UI→config→script boundary contract + the founding directive + the A/B/C preflight + the per-engine label table, reconciled vs code — corrected the source's overstatements (the full A/B/C notice pattern is wired only for the **spectra** reference engine; siesta/pyscf round-trip `regions` in metadata but don't notice them; Check C is at the render endpoint, not `preflight()`) and refreshed the stale test paths (`tests/spectra/test_engine.py` + `test_blueprint.py`). Fully consumed; prefixed `_migrated_`. |
| `protocols/slurm-integration.md` | execution/job-system.md | merged-into execution/job-system.md | Wave 5 (2026-07-27), `§ 5`. The two-layer SLURM submission model reconciled vs `runwrap.py::render_sbatch` + `jobset/submit.py`. **Stale banner corrected:** the source's "Status: design — pre-implementation, no code exists yet" is false — the emitter/per-rank launcher/timing log/monitor all ship (its own §15 marks A-D,F DONE; only §15-E test-bundle-gen pending). File shapes deferred to `job-contracts`/`running-a-job`; this doc keeps the *submission + dependency + domain-routing* mechanics. Fixed vs code: `--gres-flags=enforce-binding` is emitted (source §5 omitted it); `-c`/`-t`/mail are conditional; `config init --site` is aspirational (not shipped). |
| `protocols/staged-execution.md` | execution/job-system.md | merged-into execution/job-system.md | Wave 5 (2026-07-27). The JobSet framework contract migrated + reconciled vs `jobset/*` + `siesta/stages.py` — the sources were unusually accurate (already labelled shipped-vs-planned). Migrated: `job-set@1` model, `stages_to_jobset` ladder producer, the prep/plan/submit/status lifecycle, `on_nonconvergence`→`dep_kind`, carry-forward (D1), submit-vs-direct. §15 web integration (Phases 1-4 + D7 gate, Promotion B `write_text_within_roots` = unbuilt) → `roadmap.md` workstream 1 (was already absorbed 2026-07-26). Precision fixes carried into the doc: PySCF ladder is in-script not a JobSet; the JobSet "bundle" ≠ the handoff "bundle". |
| `protocols/structure-authority.md` | model/structure.md | merged-into model/structure.md | Wave 2 (2026-07-26): the codec + doors + metadata authority + round-trip invariant. Facts re-verified vs code — corrected the "`_shared.structure_to_dict` deleted" claim (it is a retained back-compat wrapper over `to_wire()`); §3.4 sidecar-envelope detail → `model/sidecars.md`; §6 open CLI step → roadmap. |
| `protocols/structure-load-save-contract.md` | model/structure.md | merged-into model/structure.md | Wave 2 (2026-07-26): the JS doors + model primitives + SETTLE-BEFORE-READY + consumer map → the frontend surface of `model/structure.md`. Seam facts verified vs `build.py` (`/api/build/load`, `/api/structure/save`). |
| `protocols/structure-periodicity.md` | model/structure-periodicity.md | merged-into model/structure-periodicity.md | Wave 2 (2026-07-26), sub-doc of structure.md (filename-prefix convention, R5). Reconciled vs code — **major drift cleaned up**: `kgrid` is NOT a structure field (it's a `SiestaConfig` DFT knob; sidecar v5 dropped it) → its physics deferred to `engines/siesta.md` (Wave 4), preserved meanwhile in this kept source. Also fixed: isolated cell = `bbox + 2·vacuum` (not `+vacuum`); §9 "to-build" mostly SHIPPED (capture, calibrate, resolvers); `ws.*` accessors → `molview.data.*`; transport `cell_fdf` now a legacy fallback. |
| `protocols/test-strategy.md` | process/ | pending | |
| `protocols/transiesta-workflow.md` | engines/transport.md (merged) | merged-into engines/transport.md | Wave 4 (2026-07-26). The scientific design (NEGF physics §1, the corrections §4, the recommended baseline §5, the invariant set I1–I13 §6.7 + the scientific-validation map §6.8, the data-flow §6.0, refs §8) folded into `transport.md` §§ 2, 5–7, 9 + reconciled vs code (the preflight gate ids match §6.7). |
| `protocols/ui-design-contract.md` | web/ui-contract.md | merged-into web/ui-contract.md | Wave 6 (2026-07-27). Reconciled vs the actual CSS in `static/` by a fresh-eyes digest, load-bearing facts re-spot-checked (the 5 layers + one-owner `.status` at `page-shell.css:322-325` — grep confirms sole owner — + the `692px` `@container` + the `641px` dock breakpoint + no top-level `style.css`). **The source's central running example is OBSOLETE and was dropped:** its "shared components stranded in `static/style.css`, migration pending" / "phantom overlay tokens undefined" / "four `.status` copies" debt all **SHIPPED-FIXED** (tasks #57-60/#84-85/#89) — the doc describes the CURRENT 5-layer model as shipped, not the history. Stale source numbers corrected (`@container` is 692 not 664). **CODE FOLLOW-UPS (token smells, queued):** three tokens for "dark text on an accent fill" disagree on value (`--text-on-accent`/`--accent-on`=#14171c vs `--accent-fg`=#0a1018, `tokens.css:63,250,251`); `.issues-panel` owned twice with drifting `display` (form-components `flex` vs spectra `grid`). |
| `protocols/vibrationview.md` | web/vibrationview.md | moved → web/vibrationview.md | Wave 6 (2026-07-27). **Initially folded into `molview.md § 24`; extracted to its OWN doc after a post-Wave-6 review** (the code's own headers reference `vibrationview.md`, and it is a physically-separate independent ESM module — the module-centric doctrine + task #51 both say "separate module, NOT molview"). `web/vibrationview.md` carries the mount door + handle (`showMode`/`play`/`pause`/`setAmplitude`/`setSpeed`/`dispose`), the animation model (`pos = eq + amp·cos(φ)·disp`, phase-continuous, live amplitude/speed), the eigenvector scatter (`mode-math.js`), the **semantic seal**, and the spectra wiring. `molview.md § 24` trimmed to a pointer. **ARCHITECTURE DECISION (user, 2026-07-27): full MolView/VibrationView separation** — today VibrationView borrows MolView's shared 3Dmol embed (`lib/viewer/`) via the transitional `molbuilder.viewer` global; target = the embed goes MolView-private (`lib/viewer/`→`lib/molview/`) and VibrationView grows its own minimal concealed seal. Tracked as **task #104** (docs-first: this doc + `molview.md`/`overview.md` describe today's shared surface with a current→target note; the code follow-up flips them + is cross-checked at Wave-10). **Composed-doc audit DONE (2026-07-27, CLEAN)** — all 9 claims verified TRUE vs `lib/vibrationview/vibrationview.js` + `mode-math.js` + the spectra consumer (mount door + 8-method handle, the `amp·cos(φ)·disp` model + phase-continuity, the `setAtomCoords`/`setAnimationProvider` seal, the runtime `molbuilder.viewer` borrow with no own `$3Dmol`, the 6 handle doors used + none of MolView's selection/cell/label, links); no doc fixes. **CODE FOLLOW-UP (queued, cosmetic):** `spectra/core.js:2247` comment says VibrationView "owns the canvas + standard knob bar" — stale/contradicts the module (VibrationView owns no knobs; the spectra template does); fold into task #104's spectra-wiring touch. |
| `protocols/web-api.md` | web/web-api.md | merged-into web/web-api.md | Wave 6 (2026-07-27), the server-routes reference (1812-line source). Catalogue **rebuilt from code** (`blueprints/*.py` + `app.py`) by a fresh-eyes digest — the legacy catalogue had DRIFTED. Conventions verified vs `_shared.py` (`err`/`ok_structure_response`/`workspace_payload`/`structure_to_dict`/`struct_from_body`) + `app.py` (CSP, rate-limit, 50 MB cap): the `{ok,…}` envelope, the canonical structure wire shape (`lattice` always `null`; legacy root aliases), the status map, the `_fetchEnvelope` client mirror, no-CSRF-beyond-same-origin, the always-on per-IP rate limiter (404-storm + scanner sig; `threshold_total=0` default). **Scope (b), digest-advised:** conventions + a complete catalogue cross-linking the owner module docs, with FULL shapes only for the un-owned groups (app-level, build generate/validate, checkpoint, system, docs, admin, auth). **DEAD routes flagged + listed in the doc's §6:** `/api/workingcopy/*`→`/api/state-timeline/*`, `/api/selection/save-sidecar`, `/api/selection/refresh-hash`, `/api/files/result-list` (all physically removed). **CODE FOLLOW-UPS (stale, queued):** `projects/api.js:88` "no top-level ok" comment (server sends `ok`); `rate_limit.py:71-74` docstring says "unblock" but the route is `/clear`. |
| `protocols/web-module-map.md` | web/overview.md | merged-into web/overview.md | Wave 6 (2026-07-27). The layered front-end module index → `web/overview.md`, reconciled vs the source's full inventory (framework: runtime + constants; special modules: projects, workspace, molview, selection, inspectors, viewer, vibrationview; consumers; server blueprints; data structures; loading model). Recast as a **module registry** (each module → its own doc + a VERIFIED ESM status) plus the module-independence doctrine + a layers diagram, per the user's module-centric directive. The source's `selection`/`viewer` (sub-parts of MolView) and `constants` (a primitive) are folded into their owning docs (`molview.md`, `runtime.md`) rather than given registry rows — none lost. |
| `protocols/web-ui-coherence.md` | web/ (SPLIT: ui-contract.md + tabs.md) | merged-into web/{ui-contract,tabs}.md | Wave 6 (2026-07-27). **Rule 4** (one palette / one role-name vocabulary) → `web/ui-contract.md § 2` (earlier). The **data-coherence bulk** (Rules 1/2/3/5 — two surfaces must agree about a structure's chemistry) → `web/tabs.md § 7`, reconciled vs the server enforcement by a fresh-eyes digest: one `analyze_structure` analyzer as the single source of open/closed-shell truth, findings attach to the owning workflow card, the shared `detection-chip` must match validator silence, and every new chemistry surface needs an agreement test. Fully consumed once tabs.md landed. |
| `protocols/workspace-contract.md` | web/workspace.md | merged-into web/workspace.md | Wave 6 (2026-07-27), the PRIMARY basis. Reconciled vs `lib/workspace/dispatcher.js` + `snapshot-io.js` + `state_timeline.py` by a fresh-eyes digest (every claim verified). Kept: the 10-member public surface (§3.5), the two-copy persistence (sessionStorage mirror §4.4 + the indexed server **state timeline** §4.7), owner-namespacing, the mount-restore single-authority rule (§4.5 — gate on `hasRestorableSnapshot()`, NOT `mountRestoreTarget()`). **PRUNED as DEAD:** the entire §4.6 working-copy machinery — `molbuilder/workingcopy.py` + `blueprints/workingcopy.py` + `/api/workingcopy/*` are REMOVED (`state_timeline.py:21-24` documents the extraction; only the `StructureCodec` survives), and the §4.6.6 `<stem>.<session>.wc.json` draft/orphan/recover model is dead (live timeline keys by `{workspace_id, state_index}`). Boundary restated plainly: MolView owns the data + the *when/what* to save, workspace owns only the byte transport (zero data accessors). **CODE FOLLOW-UP (queued):** `dispatcher.js:31-44` header docblock omits `hasRestorableSnapshot` from its "Public surface" list though it is exported (`:301`) and load-bearing for the §4.5 mount contract (stale in-code comment). |
| `results-tab-guide.md` | web/{results,presenters}.md | merged-into web/{results,presenters}.md | Wave 6 (2026-07-27). This was explicitly an *on-ramp* to three now-migrated specs (its own preamble: "not the authoritative contracts"): pick→mount dispatch → `results.md § 1–3`; how to write a new viewer → `presenters.md` ("adding a viewer"); the state/refresh rules → `results.md § 4`. Fully covered by the shipped docs; nothing unique to carry. |
| `roadmap.md` | roadmap.md (THE plan; absorbs scattered phasing) | pending | PLAN distilled into `docs/roadmap.md` (Wave 1, 2026-07-26) — that is now the plan authority. File retained ONLY for its shipped 3DNA detection/error/install + transport reference; relocates to `engines/` at Wave 6, then this row closes. |
| `runtime-registry-guide.md` | web/runtime.md | merged-into web/runtime.md | Wave 6 (2026-07-27). The plain-language on-ramp — its problem-scene framing, the ask-don't-grab producer/consumer pattern, and the sequence diagram — was the base for `web/runtime.md §§ 1–2` (verified accurate vs code). |
| `science.md` | **SPLIT** — §2 → science/chemistry-correctness.md; §1/§3–6 → science/overview.md | merged-into (split) | Wave 3 (2026-07-26): §2 (spin+charge science, hemeC post-mortem, cross-engine rule, auto-detect guard) → `chemistry-correctness.md`; §1 goal/principles + §3 validation-pass check catalog + the advisory-while-editing/enforcing-at-generation contract + §4 known-gaps (historical) + §5 pinned false-positive + §6 generated-output style → `overview.md`. Fully consumed; the §3 check catalog was **reconciled against current code** (open-shell-forced-closed is WARN not error; dipole fix is a larger box not SlabDipoleCorrection; spin_polarized-needs-spin_total is the propor IMAX=0 abort). Prefixed `_migrated_`. |
| `staged-relaxation-guide.md` | execution/job-system.md | merged-into execution/job-system.md | Wave 5 (2026-07-27). The user how-to (`--stage-strategy` presets loose-only/publishable/vib-quality, the step-by-step build→copy→prep→plan→run→watch, resume/branch recipe) folded into `job-system.md` §§ 3-4 + the worked CLI session; ladder *values* deferred to `engines/tuning.md`, only the *scheduling* mechanics kept here. |
| `structure-guide.md` | web/tabs.md | merged-into web/tabs.md | Wave 6 (2026-07-27). The structure-generation flow → `tabs.md § 2` ("Creating a structure — the in-gate"), reconciled vs `modify/structure/*.js` + `build.py` by a fresh-eyes digest: every source (SMILES / by-name / upload / DNA / RNA / peptide) funnels through the one gate `structurePage.loadIntoCanvas` → `installMolecule`; SMILES is RDKit-first with an OpenBabel fallback (task #54), DNA via 3DNA/X3DNA (tasks #55/#56). **Stale source corrected on migration:** it references `modify/structure/sidecar-labels.js` (does not exist — sidecar pairing moved server-side to `StructureCodec.write`) and a separate `loadStructureText()` step after the gate (current generators render via `installMolecule` inside the gate). |
| `tabs/architecture.md` | web/tabs.md (rename: avoid 3rd architecture.md) | pending | §9 phasing (Phases A-D) reconciled against code = ALL SHIPPED (6 tabs live); recorded closed in `roadmap.md` 2026-07-26. The cross-tab design content migrates in Wave 3 (web/). |
| `tabs/molbuilder.md` | web/tabs.md | merged-into web/tabs.md | Wave 6 (2026-07-27). The Molbuilder build/edit/assemble tab → `tabs.md § 2`, reconciled vs `modify.html` + `modify/viewer.js` + `modify/selection-bootstrap.js` + `modify/periodicity.js` by a fresh-eyes digest: the MolView mount lives in `selection-bootstrap.js` (`mode:"modify"`), ops route through `molview.data.applyOp` (the only direct fetch is `GET /api/modify/meta`), periodicity commits via `commitPeriodicity`. Consumer framing per the module-centric directive — MolView/selection internals stay in `molview.md`. **CODE FOLLOW-UPS (queued):** `modify/viewer.js` self-flagged redundant `refreshUndoButton()` + a TODO to drop the `window.molbuilder.loadStructureText` global; a hard-coded `modify.py:448` line-ref in a comment. |
| `tabs/results.md` | web/results.md | merged-into web/results.md | Wave 6 (2026-07-27). A self-declared stub; its only durable content (the pointer map: dispatch → results tab, contract → the registry, picker → file-picker) is now the companion links + body of `web/results.md`. |
| `tabs/spectra/references.bib` | web/ | pending | |
| `tabs/spectra/spec.md` | web/spectra.md | merged-into web/spectra.md | Wave 6 (2026-07-27). Reconciled vs `lib/spectra/core.js` (the shared engine) + `lib/inspectors/spectra.js` (the presenter) + `static/spectra/viewer.js` (the standalone tab) + `blueprints/spectra.py` (routes) by a fresh-eyes digest (two-surfaces-one-engine, chart axes, the 2 s poll, VibrationView mount, render-vs-load, ESM split all re-spot-checked). **Two surfaces, one engine** framing: the standalone `/spectrum-calculation` tab (compute→view) + the Results-tab presenter (view a `*.spectra.json`), both thin mounts of `core.js`. Chart = frequency (cm⁻¹) vs Raman activity (Å⁴/amu), Lorentzian FWHM, imaginary modes red, density-mode fallback; mode table + excited-state columns; click-a-mode → **VibrationView** (not MolView) 3D animation; the read-only MolView inspect card is a *different* surface. **Stale legacy-spec drift NOT carried into the new doc** (all verified against code): `schema_version:1` (shipped is v4), a `/api/spectra/data` poll endpoint (does not exist — live-watch re-POSTs `/api/spectra/load`), render body `{xyz,params}` (now `{structure_text,params,…}`), page route `/spectra` (shipped `/spectrum-calculation`). **CODE FOLLOW-UPS (queued, report-only):** `phase_ir` folded into `_resultsFingerprint` (`core.js:1661`) appears nowhere else — forward-looking IR placeholder; the `uiPrefs` sessionStorage roundtrip is a live TODO (`core.js:186-189`) so broadening/anim/table prefs are in-memory-per-mount (doc says so); `_apply_sidecar_if_possible` back-compat alias re-import (`spectra.py:252-258`) is a dead alias per the no-shim policy. `references.bib` left in place (a Methods-citation data asset, not a doc). |
| `tabs/structure-optimization.md` | web/tabs.md | merged-into web/tabs.md | Wave 6 (2026-07-27). The relaxation-script generator tab → `tabs.md § 3`, reconciled vs `index.html` + `structure-optimization/viewer.js` by a fresh-eyes digest: read-only MolView (`owner:"structure-opt"`) + two schema forms (SIESTA/PySCF) + detection-chip; inspect → analyze → Generate (`/api/build/fdf` or `/api/build/pyscf`, text only — there is NO `/api/build/render`) → Save through the projects door; the FDF save is the 4-step pipeline (xyz → install-pseudos → install-wrapper → psml rewrite). **CODE FOLLOW-UPS (queued):** stale endpoint comments in `viewer.js` (references `/api/build/molecule`, `/api/build/load` it never calls) + dead `state.pdb`/`state.labels`/`state.title` fields + a documented polling-anti-pattern TODO + the hand-synced `_STAGE_PRESET_KEYS_SIESTA` vs `config/siesta.py`. |
| `templates/github-workflows-test.yml` | process/ | pending | |
| `transport-guide.md` | engines/transport.md (merged) | merged-into engines/transport.md | Wave 4 (2026-07-26). The plain-language how-to (the 3-coupled-runs mental model, the `molbuilder transport` CLI, the consistency contract, shipped-vs-coming, gotchas) folded into `transport.md` §§ 1, 3, 8 — it's now the plain-language *front* of the same doc, not a separate guide. |
| `types/chemistry.md` | model/chemistry.md | merged-into model/chemistry.md | Wave 2 (2026-07-26). The source's scope (phosphate charge + protonation) fully migrated + verified vs `chemistry.py` (cutoffs 1.30/1.95, O–H 0.96 Å / 109.47°, idempotent `is`-return). Also mapped the rest of the module: charge helpers / `add_hydrogens` (OpenBabel→RDKit) / clash / dipole here; the **correctness** half (spin-charge parity, open-shell metals, ECP, `analyze_structure` + adapters) pointed to `science/` (documented there in `chemistry-correctness.md` / `scientific-validation.md`, science wave). |
| `types/parsers.md` | (retired — superseded by model/parse.md) | archived | Wave 2 (2026-07-26). The old file-level parser contract; its substance (the FileParser ABC + result types) is now in `model/parse.md` §§ 1–3. Retired stub, kept `_migrated_` for the closeout cross-check. |
| `types/structure.md` | model/structure.md | merged-into model/structure.md | Wave 2 (2026-07-26): the dataclass fields + invariants + geometry I/O (XYZ/PDB/PySCF/ASE) + TER handling + `molbuilder.load` → the L1 data-model + backend surface of `model/structure.md`. Method line-numbers verified vs `structure.py`. |
| `workspace-guide.md` | web/workspace.md | merged-into web/workspace.md | Wave 6 (2026-07-27). Body DROPPED — the guide self-flags as superseded and teaches a `ws.getStructure`/`ws.getAtoms`/`ws.selection.*`/`ws.adoptSession` data API that no longer exists on the workspace (all `molview.data.*` now), and its §5 example uses the `file !== ws.mountRestoreTarget()` gate the contract §4.5 explicitly BANS (the RNA-wipe anti-pattern). Salvaged only the plain-language on-ramp framing + the mount-restore rule (rewritten to `hasRestorableSnapshot()`) into `web/workspace.md`. |
