# molbuilder — documentation

**This is the ONE index.** Every document under `docs/` is listed here, one
line each. If a doc is not listed here it does not exist (test-enforced:
`tests/test_docs_structure.py`).

> **New here?** Start at
> [`ops/getting-started.md`](?doc=ops/getting-started.md) — from a fresh
> clone to a first calculation: which config files must exist on which
> machine (local, cluster, notifications, sign-in), in what order, each
> step handed to the document that owns it.

> **Migration complete (2026-07-28).** The previous docs tree was migrated to
> this domain-structured tree across Waves 0–9, each doc reconciled against
> the code at its move.  The old tree is archived under
> `archive/old_docs/` (see [`archive/README.md`](?doc=archive/README.md)).  The migration ledger —
> every source file → target home — is
> [`archive/MIGRATION.md`](?doc=archive/MIGRATION.md).

## Structure — domains, not document kinds

Documents are grouped by **domain** (the subsystem a reader works on), not
by kind of contract. Kind is expressed in the filename suffix and the
header, inside the domain.

| Folder | Domain |
|---|---|
| *(root)* | The spine: this index, `design.md` (mission · principles · decisions), `architecture.md` (the reuse map: task → tool), `workflow.md`, `configuration.md`, `backend-architecture.md`. **The one plan is [`plans/plan.md`](?doc=plans/plan.md)** — rule R3. `roadmap.md` and the two full-review audits were archived 2026-09-01 with the nine plan documents: 2044 lines between them, most of it closed work never struck, and their live items are merged into the one file with the fact-check that found them |
| `model/` | The data model (L1): Structure, periodicity, annotations, codecs & load/save, sidecars, region labels, selection grammar, chemistry, the parse stack, the data vocabulary |
| `science/` | Scientific correctness: validation machinery, chemistry correctness, pseudopotential standards, parameter tuning |
| `engines/` | Per-engine emitter specs (SIESTA, PySCF, transport/TranSIESTA, builders, GPU build recipe) **and the cross-engine contracts**: tier values, what a stage is, and the template every engine's parameters travel in |
| `execution/` | Running jobs: script generation & wrappers, deployment config, SLURM, the JobSet framework (bundles/ladders/sweeps), benchmarks, run layout & decoding, checkpoints, workflow handoff |
| `web/` | The whole front end: tabs, MolView, workspace, projects sidebar, results/inspectors, forms, UI/CSS contract, web API |
| `ops/` | Installing and serving the app: install model, deployment, auth/rate-limit |
| `process/` | How we work: code conventions, test strategy, audit playbook, CLI conventions, package layout |
| `plans/` | **One file: [`plans/plan.md`](?doc=plans/plan.md).** It held nine on 2026-09-01 and a fact-check found three of their headers stating the opposite of what had shipped, two of them indexed nowhere, and the same item carrying three different statuses across three files *(user: "We don't need ten plan files scattered")*. All nine are now records under `archive/2026-09-01-*`; every open item they held is in the one file. R3 now names it |
| `archive/` | History, date-prefixed. **Not a source of truth.** |

## The rules

- **R1 — one index, never lagging.** Every non-archive doc has exactly one
  line here, added **in the same commit** that adds, moves, merges, or
  archives the doc. The master index is updated at EVERY step — it is never
  allowed to drift from the tree (the structure test fails on unindexed or
  dangling entries; the index that `design.md` used to carry in its own § 0
  rotted to ⅔ coverage because
  updating it was a separate chore).
- **R2 — provenance header.** Every doc starts with a header block naming
  its **Role** (`contract` | `guide` | `overview` | `plan` | `process` |
  `investigation` | `audit report` | `reference` — the last for a **snapshot
  of an external fact** like a
  facility's topology: consulted, never binding, no number from it
  hard-coded; added 2026-08-13 when the review found three docs already
  using the word outside the list. `investigation` — a dated question
  answered against the code — and `audit report` — a dated systematic
  review — joined 2026-08-19, when the full-text review found two docs
  already carrying them), its
  **Domain** (folder), and its **Companions** (linked related docs). A
  contract is the sole source of truth for its surface; a guide explains it
  in plain language and never contradicts it.
- **R3 — contracts don't hold plans, and there is exactly one plan.** Durable
  decisions stay in contracts; every open item lives in
  [`plans/plan.md`](?doc=plans/plan.md) and nowhere else. It named `roadmap.md`
  until 2026-09-01, when that file was archived — nine plan documents plus a
  roadmap plus two audits had been holding open work between them, which is the
  drift R3 exists to stop, arriving through the rule's own front door.
  Phasing, status and open work live in `archive/2026-09-01-roadmap.md` (one pointer allowed in
  the contract).
- **R4 — one archive.** Superseded content moves to `archive/` with a
  `YYYY-MM-DD-` prefix; the archive README says what superseded it.
- **R5 — names carry the vocabulary.** File names use the system's canonical
  terms (the shared JSON vocabulary lives in
  [`model/overview.md`](?doc=model/overview.md) § 2 and
  [`execution/job-contracts.md`](?doc=execution/job-contracts.md) § 6) and must not collide
  across meanings — e.g. plain "bundle" is the JobSet framework's word for
  the portable prepped directory, so the run→next-calculation handoff doc
  had to be named `handoff-bundle.md`, never "bundle" (the handoff itself
  retired 2026-08-29 — citations replaced it; the doc is archived and
  `execution/job-contracts.md` § 5 holds the closure). **Sub-documents share the master's filename as a prefix**, so
  the hierarchy is visible in the name itself: a master `structure.md` has subs
  `structure-periodicity.md`, `structure-annotations.md`, … (a filename prefix,
  not a subdirectory — the name alone shows the parent).
- **R6 — born here.** New documents are created in this structure only. (The
  legacy tree that predated it is archived under `archive/old_docs/`; the
  2026-07 migration that got us here is closed — see
  [`archive/MIGRATION.md`](?doc=archive/MIGRATION.md).)
- **R7 — internal links use the document-module convention.** The Documents
  tab serves docs through the module (`/documents?doc=<path>`), **never** as a
  raw `.md` path — a raw relative `.md` href 404s in the rendered view. So a
  link between docs points its target at `?doc=` followed by the
  docs-root-relative path — e.g. [`model/structure.md`](?doc=model/structure.md).
  Index links follow the same form.
  Enforced by `tests/test_docs_structure.py`. (A doc that has not migrated yet
  is named in inline code, not linked — no target exists to point at.)
  **No `#` heading-anchor links** (browser-verified 2026-07-26): the renderer
  forces `target="_blank"` on every in-doc link *and* does not honor `#` anchors,
  so an anchor link opens a stray blank tab and never scrolls. For a same-doc
  section pointer use plain text ("see § 8"), not `[…](#…)`.

### The doc rule (carried over — still the point of all of this)

> Tests must be derivable from the spec without reading the implementation.
> Code reviews must verify code matches spec, not code matches reviewer's
> expectations.

## Editorial rules — how documents are written and merged

These bind every write to this tree — migration reconciles AND ordinary
edits afterwards. They are re-read at every migration gate.

- **E1 — structure first, never just append.** Before merging or updating
  a document, map the overall structure — its topics, its table of
  contents, and its siblings' — and reorganize so the information sits
  logically. Adding a new section onto old scaffolding because it is easy
  is exactly how the previous tree rotted. A merge is a re-architecting of
  the combined content, not a concatenation.
- **E2 — Mermaid diagrams wherever they explain.** Data structures
  (`classDiagram`), dependencies and workflows (`flowchart`), API designs,
  protocol sequences (`sequenceDiagram`) — if a picture explains it, the
  picture is required, and it is Mermaid (renders in the Documents tab).
  ASCII diagrams are converted when a doc is touched.
- **E3 — plain language, full rigor.** Translate jargon into simple
  language with concrete scenarios ("when you click X, Y happens") — but
  lose NOTHING: every constraint, number, edge case, and decision
  rationale survives the translation. Simplify the words, never the
  content. A coined shorthand may only be used after the sentence that
  defines it.
- **E4 — scientific content is evidence-based, and enriched.** Where a doc
  makes a scientific argument (defaults, validation thresholds, method
  choices), the physical/chemical foundation and the reference trail are
  respected — preserved on merge, and ENRICHED where thin: state the basis,
  cite the method/literature, keep defensible defaults *with their
  justification*. A scientific claim without its foundation is drift
  waiting to happen.
- **E5 — organize by the reader's surface, with worked examples.** When a
  doc (especially a merge) serves both the **Python/CLI backend** and the
  **JS/user frontend**, organize its body by those surfaces so each
  developer lands directly in their part — a clearly-labelled backend
  section (the dataclass/codec/CLI use) and a frontend section (the JS
  module / UI use), rather than one undifferentiated contract. Give each
  surface **runnable example code** (a Python snippet, a JS snippet, a CLI
  line — real, not pseudocode) and a **diagram** (E2) of how the two sides
  exchange data across the wire. The test of a good doc here is that a
  backend dev and a frontend dev each find "how do I use this, with an
  example" in under a minute. (Single-surface docs keep their natural
  structure — this binds the two-surface ones.)

## Reorganization protocol (any doc move or merge)

Moving or merging a doc is a **review gate**, not a file move. The editorial
rules above apply to every step in full:

1. Read it against the current code — fix drift or archive it.
2. Map the structure first (E1): this doc's TOC + its target-domain
   siblings'; decide the merged/reorganized shape before writing.
3. Merge overlaps with sibling docs instead of carrying duplicates.
4. Extract any plan/status content into [`plans/plan.md`](?doc=plans/plan.md) (R3).
5. Rewrite where needed: plain language (E3), Mermaid diagrams where they
   explain (E2), scientific foundations preserved + enriched (E4).
6. Add the provenance header (R2) and the index line here (R1) — same
   commit, always.
7. Repoint inbound references — other docs, code comments, tests — to the
   new path in the **same commit** (grep-verified, per file; no blind
   rewrite). `tests/test_no_retired_doc_paths.py` enforces that every
   `docs/**.md` path an active source cites exists on disk, so a move
   that strands references fails the suite.

*(This protocol ran the 2026-07 migration, whose migration-specific steps —
wave ordering, the freeze, keep-and-mark, the per-file ledger — are recorded
with the ledger at [`archive/MIGRATION.md`](?doc=archive/MIGRATION.md).)*

## Index

*(bottom-up by domain; the summary spine — `design.md`, `architecture.md`,
`backend-architecture.md` — landed last, in Wave 9)*

| Doc | Role | Owns |
|---|---|---|
| [`workflow.md`](?doc=workflow.md) | contract | **What the parts are, and how they compose** — the single contract for the WHOLE, in ordinary words: the six terms defined once, one picture of every file and what flows into it, the doors (one reader and one writer per file), the order and why each step needs the last, and where a person's answers get in. **Owns the composition, never a part's internals** — R-W1: a fact that belongs to one part is named here and defined there. Ends with the measured state of what is genuinely catalogue-driven today and what still branches per engine. Start here if the vocabulary is new |
| [`configuration.md`](?doc=configuration.md) | contract | **Every file that is *set* rather than produced, and who writes each.** A file here is either configuration or a product — *if deleting it loses a decision somebody made it is configuration; if it only costs the time to recompute it is a product* — and this page owns the first kind. Sorts all eight by **who writes it**: a person (`molbuilder.json`, `.molbuilder.json`), a probe (`environment.json`), a producing verb (`<label>.template.toml`, `task.json`, `task.1st.json`), or the engine's own package (`catalogue.template.toml`, `warm-files.toml`). Then the **three scopes** — machine · project · calculation, later wins — and the rule that a section which may not live in a scope is **refused there, never ignored** (*"read, validated and then silently dropped looks effective while nobody applied it"*). § 4 tabulates `molbuilder.json`'s ten sections with the two facts the code already keeps in one registry: which scopes may carry each, and whether its values may be printed in a provenance log — a **security boundary**, which is why seven rows say no. § 5 holds the **machine-facts split** (M-1…M-5, moved from `job-contracts.md` § 6.1a): the axis is **fact vs preference, not probed vs declared** — you can only probe the machine you are standing on, so a cluster's facts are declared when you describe from a workstation — with facts in `environment.json` in one shape whether the machine is a cluster or a workstation, preferences in `molbuilder.json`, **a probe never writes a preference**, scopes with precedence and *no field merge*, and one door reading and writing the record. R-C1 keeps it a map: it says who writes what where, never what is inside |
| [`design.md`](?doc=design.md) | overview | The design north-star — mission, the assistant-not-nanny stance, the architecture in brief, the 8 load-bearing principles, the anti-patterns we refuse, and the decisions index (full log archived) |
| [`architecture.md`](?doc=architecture.md) | reference | The reuse map — **task → tool** + the subsystem index by layer (L1/L2/L3), every row routing to its authoritative domain doc; read before building anything |
| [`backend-architecture.md`](?doc=backend-architecture.md) | reference | The **same backend by functional concern** (data · construction · validation · execution) — which concern owns each module, the cross-concern pipeline, and where the concerns still leak into each other; the paired lens to `architecture.md` |
| [`archive/2026-09-01-roadmap.md`](?doc=archive/2026-09-01-roadmap.md) | plan | The ONE plan: every open feature/backend workstream + the closed-work log |
| [`model/overview.md`](?doc=model/overview.md) | overview | The model domain's **start-here** map (every model doc + when to open it) + the atom-index convention (0-based internal / 1-based user-facing / per-engine) |
| [`model/structure.md`](?doc=model/structure.md) | contract | The `Structure` object (master): the L1 codec (`to_dict`/`from_dict`/`to_wire`), geometry I/O, the L2 paired-file door, and the JS load/save doors |
| [`model/structure-periodicity.md`](?doc=model/structure-periodicity.md) | contract | *(sub of structure)* Per-axis box behaviour: `cell` · `cell_origin` · `axis_kind` · derived `pbc` · `vacuum` (k-grid is a `SiestaConfig` knob, not here) |
| [`archive/2026-08-20-cell-plan.md`](?doc=archive/2026-08-20-cell-plan.md) | plan *(archived 2026-08-20 — all acceptance rows landed; live rules: `structure-periodicity.md` § 6.1–6.2, `molview.md` § 9.5)* | **The unit cell, and how a user says what they want** (2026-08-03) — the generated script sets up a unit cell and puts the molecule in it, so everything on the Cell page is either the cell itself or a parameter used to work one out. Two regimes, already settled in [`structure-periodicity.md`](?doc=model/structure-periodicity.md) § 6.2: **derived** (molecule + vacuum + axis kinds make the box) and **manual** (you typed a cell, and vacuum is reference-only). This plan invents no model — it **reconciles the code and the UI to the one already written**, and every claim about today's behaviour was RUN rather than read. What is right: the explicit cell wins, a vacuum edit resets to derived and says so, an origin needs a cell, the thin-gap warning reaches the tab before Generate. What is wrong: **`vacuum` is the only field in its block with no *unset* state**, so "I chose zero" and "I never said" are one value — and MolView's API already documents `getVacuum() → null where it says nothing` and `commitPeriodicityOp("vacuum", null)` clears, neither of which the model can supply; the 3 Å minimum-thickness floor **overrides values you typed and fires on molecules that are not flat** (measured); the manual-origin warning is suppressed exactly when other conditions exist; the UI gives **no** pre-commit warning though § 6.2 promises one; and the Cell page never says which regime you are in. Ends on the blocking question: every `.molstruct.json` on disk says `vacuum: [0,0,0]`, which the new rule would read as a deliberate zero |
| [`model/structure-annotations.md`](?doc=model/structure-annotations.md) | contract | *(sub of structure)* The per-atom channel model (`tag`/`flag`/`value`; `regions`/`frozen` built-ins), persistence, engine translation, and the region-label vocabulary |
| [`model/structure-molstruct.md`](?doc=model/structure-molstruct.md) | contract | *(sub of structure)* The `.molstruct.json` save file: envelope (`schema_version`/`structure_hash`/…), schema versioning (v3–v6), the codec, and the `.xyz`↔sidecar pairing rule |
| [`model/chemistry.md`](?doc=model/chemistry.md) | contract | The chemistry helpers on a `Structure`: net-charge resolution (phosphate heuristic + override), protonation, `add_hydrogens`, clash relief, dipole (correctness machinery → `science/`) |
| [`model/parse.md`](?doc=model/parse.md) | contract | The unified read stack: three ABCs (File/Text/Dir parsers), the frozen `ParseResult` hierarchy, the registry (`detect`/`parse`/`parse_dir`/`parse_text`), package layout, plugin + composer contracts |
| [`science/overview.md`](?doc=science/overview.md) | overview | The science domain's **start-here** map + the two cross-cutting rules: validation is advisory-while-editing / enforcing-at-generation (`report()` is the gate), and the validation-pass check catalog (geometry / cell / k-sampling / spin-charge / field-range) with rationale |
| [`science/validation.md`](?doc=science/validation.md) | contract | The runtime scientific-validation machinery: the engine-agnostic chemistry analyzer (`analyze_structure`→`ChemistryAnalysis`), the 3 noble-metal categories, the per-engine adapter registry, the consumers (`check_open_shell_metal` + `/api/structure/analyze`), and the `validation/` package layout |
| [`science/chemistry-correctness.md`](?doc=science/chemistry-correctness.md) | contract | The chemistry control surface end to end: the 5 points where chemistry can go wrong, the `(charge, spin)` science (conventions + coordination-dependent spin), the pure primitives (`total_electrons`/`check_spin_charge_parity`/`detect_open_shell_metals`/`explain_metal_spin`), the hemeC-dithiol post-mortem, and the per-step audit checklist |
| [`science/pseudopotentials.md`](?doc=science/pseudopotentials.md) | contract | The `.psml` pseudopotential coverage checks (C1–C6): the two entry points (SIESTA preflight + `molbuilder pseudo check`), the ERROR/WARN severity model (incl. the XC family-vs-author split), the dead-KB-projector guard, what is *not* checked, and the KB/ONCVPSP/PseudoDojo references |
| [`science/junction-cell.md`](?doc=science/junction-cell.md) | contract | **How long the junction's box is along z, and why one interlayer spacing decides it.** The atoms' extent alone puts the bottom atom's periodic image *exactly* on the top atom, so the rule is `z_span + d` (`cell.bulk_z_period`, shared with the electrode wizard). Then the two conditions padding does *not* buy: the layer count must be a whole **stacking period** (3 for fcc(111) ABC, 2 for (100)/(110) ABAB — with the measured warning that a distance check alone passes a **twin** at 2 or 5 layers), and the `-z` slab must be **translated, not flipped** — mirror, C₂ and inversion all leave the two faces on the same registry, because each close-packed layer is itself a centrosymmetric 2-D lattice. § 4 splits the cost by run: a plain periodic SIESTA run carries a contaminated boundary density, while TranSIESTA's open axis (I8) replaces the seam with Σ, so what actually matters there is the **bulk electrode cell**. Ends with an audit of the shipped Au-BDT-Au |
| [`engines/siesta.md`](?doc=engines/siesta.md) | contract | The SIESTA `.fdf` emitter (`render_fdf`/`convert`): the 16 output sections, the charge / spin / cell contracts (there is no `cfg.cell_padding`; vacuum adequacy is reported, never fixed), k-grid & lattice, the `Diag.Algorithm`/ELPA/GPU emit contract, staged optimization, and the sibling `.molwatch.log` / Makov-Payne / memory modules |
| [`engines/pyscf.md`](?doc=engines/pyscf.md) | contract | The PySCF `.py` script emitter (`render_script`): output files, the logging / optimizer / spin-method / charge contracts, the engine-agnostic molwatch-log format, in-script staged optimization, and the publication-quality parameter guide (tiers, basis/functional, methods-section template + citations) |
| [`engines/transport.md`](?doc=engines/transport.md) | contract | The TranSIESTA / NEGF transport engine: the COMPOSITE model (one citation → five stages: seed · electrode_L · electrode_R · device · transmission), the NEGF physics, region-label-driven electrode derivation (`TS.Elec`/`TS.ChemPots` emit), the cross-run consistency **invariant set** (I1–I13 → preflight gates), the scientific baseline, and the Brandbyge/Papior/Xiao references |
| [`engines/builders.md`](?doc=engines/builders.md) | contract | The structure-synthesis contract: the five builders (peptide / DNA / RNA / SMILES / name), the sequence-notation grammar (`[SEP]` brackets, 5′/3′), the pluggable nucleic-acid backend registry (threedna / amber / rdkit), backend-quirk repairs, the mismatch-clash policy, the tri-state hydrogen control, and the CLI + Python surfaces |
| [`engines/tuning.md`](?doc=engines/tuning.md) | contract | The cross-engine optimization-quality dial: the four-tier framework (screening → publishable → tight), per-parameter guidance (optimizer, force/SCF tolerance, mesh/basis/k-grid, step caps), the shipped SIESTA + PySCF three-stage ladders, the cross-engine parameter map, restart strategy, and the scientific references behind each default |
| [`engines/stages.md`](?doc=engines/stages.md) | contract | **A named parameter set over one system, and the file that describes it.** No engine has a concept of a stage — SIESTA reads a `.fdf` and has no idea it is the second of three — so a stage is molbuilder's own device: *a named set of the parameters a mission tunes, laid over the shared description of the system it does not*. **Three fields: name, enabled, overrides.** The **effective config** is `base ⊕ overrides`, an ordinary `SiestaConfig`, and two rules keep it from becoming a special case: one object is validated *and* rendered, and **a stage is validated as a resolved whole, never as a diff** (two overrides can each be reasonable and jointly under-converged). A promoted field lands in one of three places — an ordinary deck line; a deck line that is *also* a resource decision (`Diag.Algorithm` picks the numerics **and** the wrapper's conda env); or a field the deck never carries. Persists as `task.json` (`molbuilder/task@1`): unknown keys **refused rather than ignored**, absent `stages` **refused** too (§ 6.5 — a job always has at least one stage, and one stage is named and tokened like any other), **one reader for both surfaces** — which is what makes *“the web writes the same bytes as the CLI”* checkable rather than coincidental. Two limits stated rather than glossed: one deck per stage is **SIESTA's** shape (PySCF's ladder runs inside one process, so three stages are one file), and a produce is **transactional** — built elsewhere and moved into place only when every deck, wrapper and description succeeded. § 7 covers the folder a produce writes, the two boundaries where a checkpoint is taken, and how a mission **grows**: the stage list is appended to, three rungs is a default and not a bound. **Names are stable, positions are not** — a stage's position must never reach a filename, or inserting one silently reassigns outputs that already exist |
| [`engines/template.md`](?doc=engines/template.md) | contract | **A calculation's parameter catalogue — the file that carries it from a browser to whatever machine will run it.** Every parameter the engine's schema declares, each with the value in force and everything known about it; `task.json` beside it says only what *changes*. It is a **floor-2 description object** (`execution/architecture.md` § 2.1), and that placement decides the contents: floor 2 **must never name a machine**, so a rank count, a queue or a partition is not an item — **but `BlockSize` is** (decided 2026-08-11): it is a tunable SIESTA keyword whose *value* may be unset, and `prep` proposes one — it is resolved at `prep` on the target. **It is a TOML file, not an `.fdf`**, and the reason is structural rather than a preference: a deck is a floor-3 product rendered by `prep` step 3, so a description that literally *is* a deck carries output from a floor above it — and being an `.fdf` stored each value **twice**, in the declaration and in the payload line beside it, so the file could disagree with itself. TOML stores it once, has a spec and a stdlib reader from 3.11, carries multi-line prose, and cannot be restructured by a stray indent. **Every item declares its `kind`** — `engine` · `deck` · `wrapper` · `produce` · `monitor`, a closed vocabulary — so a layer filters on one key instead of carrying a field list, and a producer refuses cleanly rather than emitting someone else's parameter as a keyword. **`read_by` is the second question**: `kind` says who owns the payload, `read_by` says who *else* derives from the value (`Diag.Algorithm` is the engine's **and** picks the wrapper's conda env) — which replaces the wrapper sniffing deck text for ELPA. **Membership is total**, so nothing is a judgement call: every parameter is an item, with three exclusions each carrying an existing rule (a machine fact, the ladder, the structure). **Losslessness is per `kind`** — `engine`/`deck` round-trip exactly, checked by rendering a stage's deck both ways and comparing text; the rest must be carried and legible. **§ 8 traces the deck's five reserved comment blocks back to their sources**, because two of them lost theirs when the deck moved to `prep`: ATOM-METADATA's labels ride with the **structure and its sidecar** (a label is a fact about atoms, not about a knob — and the sidecar's `frozen_atoms` stays distinct from the form's `frozen_indices`), while USER-CUSTOM becomes **an item**, because the read-the-previous-deck merge has no previous deck at `prep`, no single target across N stages, and would make `prep` unreproducible. The single-deck paths keep that merge; the staged path carries the text. Supersedes `job-contracts.md` § 3.7 |
| [`engines/overview.md`](?doc=engines/overview.md) | overview | The engines-domain **map** (what siesta / pyscf / transport / builders / tuning / stages / template each own) plus the **two** cross-engine contracts it owns *(the template is large enough to have its own file)*: the shared **script-contract wrapper** (provenance / ATOM-METADATA / user-custom), the **UI→config→script boundary-condition contract** (no silent absorption — the spectra frozen-atom reference instance + the A/B/C preflight), and the shared **staged-optimization map** — what the two ladders share is the **tier values** (`engines/tuning.md` § 4); the non-convergence policy is **PySCF's alone**, corrected 2026-08-11**; plus the engine registry for adding a new engine |
| [`execution/overview.md`](?doc=execution/overview.md) | overview | The execution-domain **start-here** map. Routes the execution docs plus `engines/stages.md` and the web plans, grouped by **what kind each one is** — a *contract* says what a thing is and wins any disagreement, a *guide* says how to do it with what ships today, a *plan* is the only kind allowed to describe something unbuilt — with a dependency diagram whose arrows read *"is defined in terms of"* (`job-contracts.md` has none outgoing, which is what makes it the ground). Then **the two transitions the domain is shaped by**: *which surface* can run a job — single-task works everywhere, the JobSet framework works from the CLI, the job system in the browser is the target, with a shipped-vs-planned × CLI-vs-web **status matrix** — and *what the folder looks like*: the two shipped shapes, **flat** (stages by filename suffix, attempts by output index, **warm files unsuffixed and shared**) and **hierarchical** (a directory per stage, per attempt), both selectable in the description. § 2.1 puts the two side by side on the question that decides between them: *after three stages have run, what do you still have?* Flat keeps its history **in time**, in the checkpoint; hierarchical keeps it **in space**, on disk. So a missed checkpoint is a thinner history in one shape and **a state that no longer exists anywhere** in the other |
| [`execution/project-layout.md`](?doc=execution/project-layout.md) | contract | **What lives where in a project directory, who puts it there, and which of the two shapes you are in.** **§ 1.0 defines the run directory functionally** (user, 2026-08-11): it *hands the engine everything it needs and holds everything it produces* — that is what makes it one, not which files are in it. **Every file in it has one of two origins**, rendered out of the template or copied in as raw data, so the inventory is derived rather than maintained. What is kept **outside** it is the *starting point for rendering* — template, description, data — because **the engine does not understand the layer we use to organise our information**. So **the directory boundary is where the translation happens** (`architecture.md` § 10 names the rest): above it our vocabulary, below it the engine's plus raw bytes, and `prep` is what crosses it — which makes **the two shapes two answers to one question, is that boundary a directory wall or a filename convention**. Hierarchical builds the wall; flat does not, which is exactly why its results overlap on purpose. A follow-up stage takes **two** inputs: the parent's template and the named run's latest results. § 1: a project directory is **flat** or **hierarchical**, and *both hold several stages and several attempts* — they differ in **how those are kept apart**. Flat tells stages apart by a filename suffix and attempts by an output index, with the warm files **unsuffixed and shared**; hierarchical uses a directory for each. The shared warm files are the flat shape's whole design, good and bad: unsuffixed *on purpose*, because that is what lets stage 2 find stage 1's geometry with no instruction from anyone — and exactly why stage 2 overwrites it. **The trade is stated on the question that decides it**: after three stages have run, what do you still have? Flat keeps one state on disk and its history **in time** (in the checkpoint); hierarchical keeps every state **in space** (on disk, openable). So a checkpoint is insurance in the hierarchy and **the mechanism** in the flat shape — the only way back. Neither shape is wrong, and **the shape is a required field in the description** — declared when you describe the calculation, read by `prep`, because a shape chosen at the first prep and not written down is one the second prep cannot know. § 1.3 reads the whole contract against both in one table so the flat case is not a footnote — including the row worth reading twice: *a result is never overwritten* holds for stdout and **breaks for warm files**, and that break is what makes continuing free. **§ 2 — the workflow.** The browser writes a **portable package** that names no machine; **`prep`, on the machine that will run it**, resolves the machine and renders the deck. That split is forced: a deck carries values that depend on how it will be launched — a block size derived from the rank count, an eigensolver that also picks the wrapper's environment — and **a parameter that depends on the launch cannot be decided before the launch is known**. **§ 2.3: `prep` is a framework, not a step.** Five moves, always in this order — resolve the machine, resolve the effective parameters, render the deck, render the wrapper, build the run directory and copy in what was named. What differs between jobs is only *where parameter values come from, how many decks, and what gets copied in*, which is why **benchmarking is prep whose parameters are a set rather than a point**, and why a third kind of prep needs nothing new at the top. The three jobs are worked through: measuring (trials relabelled and forced cold, so they *cannot* damage the real run), the real run (one measurement, three destinations), and **continuing from an earlier run** — `.XV` always, `.DM` when the description says, `.CG` only when both stages share an algorithm; **copied, never linked**, because the engine writes to them and a link would destroy the result you chose to build on; copied **at prep**, which follows from stages not chaining rather than being a separate choice. **Stages do not chain** (§ 1.6): a stage is a long job, and a chain that continues by itself can spend a week refining a geometry you would have rejected in a minute. **An attempt is immutable** (§ 1.5). **A benchmark nests inside the stage it measures**, because the best rank count depends on the science. **§ 2.7 — the return leg, the largest gap**: every file movement in this design points *outward*, and four promises quietly assume the results are where the browser is. **§ 7: twenty invariants of the TREE** — paths, ownership, naming — each marked `[both]` or `[hierarchical]`; the *history's* invariants are `checkpointing.md`'s and are cited, not repeated. Six open questions, including how a user *asks* for the shape |
| [`execution/job-contracts.md`](?doc=execution/job-contracts.md) | contract | The stable on-disk formats + shared vocabulary every surface rests on: the **run-directory layout** (one-job basename rule, file catalogue, multi-stage suffixing, the directory discovery chain, the `project/topic/calculation` tree + the nine canonical topics, the `.run.sh`/`.sbatch` wrapper), the **generated-script reserved blocks** (HEADER/PROVENANCE/BENCH-MARKS/ATOM-METADATA `v4`/USER-CUSTOM + the per-engine emit matrix), **warm/cold restart** (the four behaviours, per-engine warm-file inventories, the status banner), and the **data vocabulary** (persisted-artifacts registry, the `@major` schema convention, the config↔scheduler parameter translation, identifier conventions). **Who writes each configuration file, and at what scope, is [`configuration.md`](?doc=configuration.md)** — § 6.1a's machine-facts rules moved there 2026-08-17 so this registry answers one question rather than two |
| [`execution/architecture.md`](?doc=execution/architecture.md) | contract | **Who owns which decision inside the execution domain.** Two questions that are not each other's answer: *who may know about whom* (**seven floors** — names, description, plan, layout, launch, observe, surfaces) and *what happens in what order* (**four routes** — produce, prep, submit, observe). A floor may call down and never across; a route crosses floors in an order it chooses, and that order is allowed to disagree with the floor numbers — which is why `prep` is a route rather than a floor, and why treating it as one once left *"resolve the machine"* with no owner. Also answers two questions no document held in one place: **which config section reaches which part of the system and at which step** (§ 8 — all **ten** sections, where `deployment.md` showed six and `running-a-job.md` four, neither saying it was a subset), and **what differs between a workstation and a cluster** (§ 9 — the floors and routes are identical; what changes is what floor 1 detects, whether a `scheduler` block is needed, and whether `launch` shells out or queues. The inner `.run.sh` is byte-identical either way, which is why a run debugged on a laptop is the run the cluster performs). Carries the per-floor **entry points**, a class diagram of the objects that travel between floors (each answers one question, once, and has exactly one builder), the five steps of `prep` with a worked example naming who decides each thing, the end-to-end workflow, and rules **A1–A7** with the test that checks each. **§ 10 adds the third structural view: the nine VOCABULARIES and every point where one becomes another** — one fact is `omp_threads` to a scientist, `cpus_per_task` to an exchange file and `-c` to SLURM, and each rename is where drift enters. **Ten of the eleven translations are derivable** — the mapping rides on the thing being translated (an item's `anchor`, a label handed to `identity`), so **adding an engine adds items, not translations**; the one genuine exception, config ↔ scheduler, is independent languages, is the single maintained table, and is the one that actually drifted. Closes with the failure it exists to catch: **a translation rule written in two documents gets fixed in one**, with the three 2026-08-11 cases named. Status lives in `archive/2026-09-01-roadmap.md`, not here |
| [`execution/generator.md`](?doc=execution/generator.md) | contract | **How declared data becomes a job set** (2026-08-11) — the data spine from the engine schema through the template to the rendered decks, `ParameterSet` and why a run is a sweep of length one, what bounds a sweep, the layered module map, and the engine seam. |
| [`execution/script-preparation.md`](?doc=execution/script-preparation.md) | contract | **The preparation layer — three questions, and only three.** Preparation has exactly three structural questions and this document is those three. **Space** — the seven floors, with L1/L2/L3 shown as the *same axis coarsened* so a test can enforce it, plus the mapping and its one deliberate straddle (`task` L1 beside `template` L2, so a description stays readable without pulling in the engines). **Time** — one sequence at three resolutions: the twelve-row decision chain zooms to `prep`'s five steps, which zoom to the thirteen sub-steps of step 3, with every adjacent pair justified by the data dependency that forces it (the structure is loaded before the data files are copied, because the element list is what says which files are needed; the deck is finished before its wrapper, which reads it for the environment and the rank clamp). **The seam** — fifteen questions an engine answers, one at step 2 and fourteen inside step 3, so **step 3 is the engine's step and steps 1, 4 and 5 are the framework's alone**, and an engine author reads one section. Two separate gates guard it — **validate**, the existing config framework shared with the UI preflight, and **check**, new here, which reads the written file exactly as the engine will open it and catches the writer bugs a config gate structurally cannot. Three rules govern step 3: a value is written together with the reason it holds, read from where the value came from; nothing reaches a deck until the last thing that can refuse has refused; one deck is written by one writer, which keeps what the reader put in their own section. `runwrap` sits on floor 3 with the other renderers and **`prep` is not a floor but the conductor** — it walks 1 → 4, owns no decision, and only a surface may import it. The rule binding the two axes: **step N runs on floor F(N), and F never goes backwards.** Ends with the BDT ladder traced through the whole sequence; what the code does not yet do is `archive/2026-09-01-roadmap.md` § 6, P1–P6, and the rebuild that closes most of it is `archive/2026-08-18-preparation-backend-plan.md` |
| [`archive/2026-08-18-preparation-backend-plan.md`](?doc=archive/2026-08-18-preparation-backend-plan.md) | plan | **Building step 3 new, and migrating both engines onto it** — the five phases that make the code match `execution/script-preparation.md`. Rebuilds **step 3 only**: steps 1, 2, 4 and 5 are already engine-neutral and rebuilding them would put `resolve` and `materialize` in two homes. The safety net is **not** byte-compatibility with the old output — reproducing the old bytes would preserve the 808 hand-typed comment lines this exists to end — but a narrower and stronger set: same files from the same places, each through its **one API**, **no value emitted except through `parameter()`** (W2, mechanically checkable), documented artifact formats, and a deck that runs and is scientifically right. The framework grows **inside `script_emit`**, where `Parameter` already lives, rather than in a new module. The engine gets exactly three doors — syntax for one parameter, the structure block, and a free-form body for what no parameter models — and the evidence for needing both the per-parameter and the free-form door is PySCF's run loop, which is program structure rather than parameters. **PySCF goes first for an architectural reason, not convenience**: building on SIESTA first yields a keyword-list abstraction that PySCF is then bent to fit, and PySCF has zero blast radius because it has no seam entry yet. Each cutover **deletes the old writer in the same change** — no shims, no two writers for one engine. `convert()` is re-pointed rather than deleted (a shipped CLI path); `spectra` and `transport` are explicitly out of scope. The **49 test files** touching the two writers are classified per phase into property-of-the-artifact (kept as the gate), old-text-shape (retired with the reason named), and asserts-a-rule-no-document-states (retired). Closes roadmap P2–P6 |
| [`execution/asu-sol.md`](?doc=execution/asu-sol.md) | reference | **The facility molbuilder actually runs on** (read 2026-08-11) — ASU Sol's node types, partitions and QOS, how resources are asked for, and the three things it settles: a domain is a (partition, QOS) pair, a GPU node has 48 cores against a standard node's 128, and `htc`/`public`/`general` share silicon so a benchmark's `choice` carries between them. **A snapshot, not a contract — no number here may be hard-coded.** |
| [`execution/checkpointing.md`](?doc=execution/checkpointing.md) | contract | **The invariants a run directory's saved history must hold** (2026-08-06). **Thirty-one**, each written so a test can assert it, each marked `[both]` or `[hierarchical]` for the directory shape it holds in, and each paired with the failure it prevents and the check that catches it. Why a contract and not a guide: replacing a produce, redoing a stage and forking a what-if all assume the history is complete and honest, and **a history with a hole in it is worse than none, because the hole is invisible until somebody needs what was in it**. Five groups — **separations** (a regular file is git-tracked XOR content-archived, never both and never neither; a stage writes only inside its own directory; the description is input and is never modified by a run), **immutabilities** (an archive's content is never rewritten; every MANIFEST entry matches its file by name, size and sha256; warm files are moved aside and never deleted; **no generated wrapper contains git**, since a wrapper that committed would need git on the compute node), **atomicity** (build-verify-swap; restore verifies the target archive before touching the worktree; the checkpoint precedes the mutation it protects), and the **layout** rules L1–L8. **§ 8 is the one to read first**: in the flat shape each stage overwrites the last's warm files, so the checkpoint is not insurance — it is **the only way back**, and a missed one is a state that no longer exists anywhere. **Status (§ 12): twenty-five of the thirty-one are held today**; four (L1, L2, L5, L7) were found broken and were fixed, three of them losing data rather than disk; **L6 is not held** — the archive-verification code exists but is reachable only by attempting a restore, which is the worst moment to learn an archive is gone. Closes with a reading ledger recording what was checked against the code, what was wrong, and which defects only a reading pass could have found |
| [`execution/preparing-for-another-machine.md`](?doc=execution/preparing-for-another-machine.md) | contract | **Probe there, prep here, run there.** Which machine a `prep` is *for*: `--target` supplies the target's MEASUREMENTS (cores, scheduler, reachable `(partition, qos)` domains) so a bundle can be prepared on a laptop and rsync'd to a cluster. The four ways the wrong machine could be chosen **silently** and which are refused; what travels with a bundle and what does not — in particular the **bootstrap**, which is a fact about the machine and therefore rides its probed record, so `prep --target sol` bakes Sol's `module load mamba` and not this box's conda hook (it read the other way until 2026-08-24, and § 3 keeps the retraction); and what the browser may offer |
| [`execution/scheduler.md`](?doc=execution/scheduler.md) | contract | **What a machine offers, and what a job may ask of it.** The subsystem that owns admission (does this request fit this queue), placement (which queue it goes in), and the directives that placement produces — written after two ASU Sol failures three days apart turned out to be one defect seen twice: scheduling work spread across five modules with **two independent emitters**, so a `#SBATCH` header could name a 15-minute queue while the command line asked for 38 minutes. Eight rules, of which the load-bearing ones are **R1** (a placement is decided once; the header and the flags are two renderings of it, never two decisions), **R2** (admission is total — every constraint the record carries is compared, or the record should not carry it; `max_mem_gb` had been declared, serialised and read by no code at all), **R5** (a header that names a queue states a wall that queue accepts — otherwise `sbatch` by hand inherits a partition default the named QOS forbids), and **R6** (refuse before submitting when the record already says the scheduler will not take it). Grades its own pieces honestly: admission, placement and emission are forced by measured defects, the record and probe modules are relocations that give them a home |
| [`execution/submission.md`](?doc=execution/submission.md) | contract | **What a job asks for — where every number came from, and how you agree to it.** Written after a 38-minute job was sent to the 2 TB partition because a memory number nobody chose did not fit anywhere cheaper. **Five provenances** — declared · measured · derived · bounded · **assumed** — and the rule that an assumed number is **announced by name** at the moment of choosing (**S1**), never buried. Why a benchmark is **bounded and never estimated** (it exists to measure the cost; it cannot be told the cost), with the quick-look/full-run choice and the `debug` fit check. Why a measurement is **not portable** across a **machine** (**S3**) — the only one of the four guarding science rather than a resource, and the one that stayed dead longest: it read a scalar `node_type` no probe could honestly write, and its refusal was finally deleted in 2026-09-04 when the route it guarded turned out to have been retired (`scheduler.md` **R0**, **R11**). The **three statements** that must agree — asked ⟷ script ⟷ scheduler (**S5**) — which is the check that would have caught every GPU trial emitting `.false.`. And the gate: nothing submitted unseen, `--yes` to accept, per-job otherwise |
| [`execution/gpu.md`](?doc=execution/gpu.md) | contract | **How a GPU decision travels — one question, ten names.** One tick-box becomes an environment, a scheduler flag, an MPI launch, a memory cap and a NUMA pin, and between those points it is read under ten different names. Written after five GPU defects in one month turned out to be one bug seen five times — every one a GPU fact with more than one home. Names the **three answerers** and the line everything turns on: `use_gpu` is the person's choice (**G2** — the `allocation` set is exactly `mpi_np`, `gpu_count`, `omp_threads`, `max_memory_mb`, `threads`), the eigensolver decides no environment and no resource (**G3**), and the **ask** (`gpu_count`) is a different variable from the **ceiling** (`Device.per_node`) that bounds it (**G4**). Carries the one decision graph from tick-box to running job, the ruled-but-unlanded `use_gpu` → `use_gpu` merge with its 183 sites, and the four places the tree currently disagrees with itself |
| [`execution/running-a-job.md`](?doc=execution/running-a-job.md) | guide | The usable single-job path (web single-task + CLI): the self-contained **standalone / detection contract** (T/M/C baked at generate, only allocation + hardware read at runtime), **runtime resource resolution** (MPI/OMP precedence + the `n_atoms` rank clamp — a **heuristic proxy**, corrected 2026-08-11: `propor: IMAX = 0` depends on the species count and radial-table size — GPU load-balance / MPS / NUMA pinning), the wrapper flags, **watching a run** (the backgrounded monitor + SCF timing + the propor/IMAX hint + the shipped `run_status` verb), **`molbuilder.json`** config (`script_generation` / `scheduler` / `execution` / `envs` + the `.sbatch` header sources), and **checkpointing** (`molbuilder checkpoint` git + `.binsnapshots`) |
| [`execution/run-reports.md`](?doc=execution/run-reports.md) | contract | **How a job tells you it is going** — the monitor's notifier, and the one sentence the design follows: *when* to speak belongs to the calculation, *where* to speak belongs to the machine, and they are never in the same file. A `task.json` travels; a webhook URL is a credential (for Slack and Discord it IS the credential), so the policy is a field of the description and the destination is a `0600` file in the config directory. Three occasions — an SCF cycle converged, every N hours, and it ended, the last not settable because switching it off removes the one message the hook exists for. Looking and speaking are different cadences: sampling stays dense for `util.csv`, while a notifier that fired per sample sent a message every ten seconds for the length of a run |
| [`execution/run-identity.md`](?doc=execution/run-identity.md) | contract | **The name that decides whether a calculation continues** (proposed 2026-08-06). The `SystemLabel` / `JOB` literal keys every warm file, and `job-contracts.md § 2.1` Rule 2 makes every stage in a folder share it — so **continuing is not something molbuilder does**, it is what the engine does when it finds state keyed by the id it was given. molbuilder's whole contribution is to make the id right and put the files where the engine will look. One rule: **the id is built from inputs, never from anything a run produced** — it must be knowable before the calculation exists, and an id that depended on a result would break precisely when the calculation worked. What is left is *which atoms these coordinates belong to*, plus the user's own name for it: readable, not cryptographic, and deliberately blind to everything a stage tunes (that blindness is what makes several stages one calculation). Normalisation reuses the shipped basename set rather than inventing one, happens **once**, and refuses rather than appending a digit; the id also names the innermost folder, so a listing identifies its contents. **Every engine has an internal identity and parameters bound to it** — `SystemLabel` + `DM.UseSaveDM`/`MD.UseSave*` for SIESTA, the `JOB` literal + generated resume branches for PySCF — declared as one group per engine and set from one `restart` field, because the two ways they can disagree (honoured with nothing to load; present but not honoured) are both silent. Closes with what is **reported rather than pinned** — a changed cell is not a mismatch but a silent no-op, since a `.XV` carries its own cell and **wins** on a continue — each case naming who says it and when (the reader at preflight, the surface at check time, the wrapper banner at run time, the last being the one that must never be weakened because it is always present); and what happens on a second produce: refuse unless told to overwrite, never rename — the warm files already there are the point |
| [`execution/worked-example.md`](?doc=execution/worked-example.md) | guide | **The whole design followed once, end to end** (2026-08-06) — one molecule (benzene-dithiol on gold) taken from a structure file to a published geometry, in the order a person would actually do it: save the structure, describe the calculation as **base + a coarse stage + a tight stage**, generate the folder, **measure** the hardware before spending a week on the expensive one, run both, save a checkpoint at each converged step, then branch to a third stage two weeks later. Written for the second reason as much as the first: **a walkthrough crosses every seam, so it finds what reviewing a module cannot** — a join is invisible from either side of it. It found **eight gaps**, four of them on no list before it was written, and § 8 ranks them. The serious one is a **regression I introduced myself**: `materialize` links a stage's carry to `../<stage>/<id>.XV`, but attempt directories moved every output down into `run-N/`, so the link now dangles — and *which* attempt is the good one is not knowable when `prep` runs, because the runs have not happened. The fix removes machinery rather than adding it: **stages stop being chained**, so the hand-off is a plain file copy made when you set the next stage up (`project-layout.md` § 1.6) — the run it comes from has already finished, because you just looked at it. **The deeper finding is about my own first fix**: the shell block I added for attempt directories is `jobset/materialize.py` rewritten in bash one level down, in the one layer kept free of filesystem work. Then I designed an elaborate way to make a chained hand-off correct when the answer was not to chain. The system was already built the right way — `materialize` lays out directories and links, `submit` picks the working directory and launches, the wrapper activates and execs — and both rules are now written where they can be pointed at (`running-a-job.md` § 2.2a, `project-layout.md` § 1.6). |
| [`execution/job-system.md`](?doc=execution/job-system.md) | guide | The JobSet batch/staged/HPC framework — **CLI-shipped; the web describes and observes**: the `job-set@1` model (`JobSet`/`Job`/`Resources`/`WarmFile`, sweep vs ladder, **no edges between jobs**), floor 3 written by `prep`'s five steps (*the chaining producers died in the 2026-08-12 fold*; the PySCF ladder is an in-script loop, so its JobSet has **one job** whose deck holds every rung — a JobSet of length one, `stages.md` § 1.1a), the `molbuilder jobset init/prep/plan/submit/summarize/status` lifecycle, **SLURM deployment** (routing domains, submit-vs-direct, **one job per invocation**), the **benchmark workflow** (`environment@2`/`bench-result@1`, the `(G,K,c)` grid, and the winner — **no wall or memory is sized**, deleted 2026-08-24), and the **target web migration** (roadmap workstream 1, D7-gated) |
| [`archive/2026-08-19-template-unification-plan.md`](?doc=archive/2026-08-19-template-unification-plan.md) | plan | **The template becomes one file for every engine** (2026-08-13) — the plan of record for `engines/template.md`'s `@2` shape: a template describes a CALCULATION, not an engine, so one file carries every engine it can run on and items are **grouped, never merged** (`net_charge` and `charge` stay two items in one category — merging would demand a shared physics vocabulary and risks fusing `dm_tolerance` with `scf_conv_tol`, which read alike and cannot take each other's values). Three orthogonal axes: `category` (which question about the calculation — a closed six, in reading order), `engines` (whose vocabulary), `kind` (which layer consumes it). Two boundaries are argued rather than asserted: **accuracy is what answer you will accept, convergence is how to reach it** (one panel holding both invites loosening `conv_tol` to quiet an oscillating SCF), and **`execution` is the sweepable set** because those knobs change speed and not the answer. Carries the measurements that motivated it (`SiestaConfig` ∩ `PySCFConfig` = **3** field names, all molbuilder-internal; three of four engines cannot render a template at all), the rule that reconciles execution items with floor 2's machine ban (**the template may declare the QUESTION, never assert the ANSWER** — valueless items, `prep` resolves from `environment.json`), the nine units T0–T8 in order, five open questions with a position on each, and the six invariants a review checks. |
| [`archive/2026-08-19-staged-runs-implementation-plan.md`](?doc=archive/2026-08-19-staged-runs-implementation-plan.md) | plan | **The order the code follows the contracts** (2026-08-07) — the build plan for the staged-run system, bottom-up through thirteen milestones, and the **only** place the order, the gates and the per-item acceptance criteria live (the design is `execution/architecture.md`; the flat-shape draft it grew out of was archived 2026-08-11). Built on one rule: **the contract is the source of truth and the code is evidence of the past** — so every phase opens with a **re-anchor** that names the exact sections to read *with every code file closed*, writes the obligations out **before** opening a diff, and names the governing sentence for each unit. Three outcomes when code and contract disagree, and choosing is the phase's first act: the code is wrong (change it), **the document is wrong (stop — change the doc in its own commit, then the code)**, or it is undetermined (a decision, and decisions are the user's). The layers are a stack read as *"cannot be written until"*: the description (`task.json`, one reader) → resolution (the template's values ⊕ a stage's overrides, validated as a resolved whole) → identity and rendering → the four acts (produce · prep · submit · the history) → the surfaces last, because **the backend must be able to render a stage that overrides a parameter the stage type never carried before any of it is drawn**. Every milestone gets **three review lenses that are different readings, not the same one thrice** — *does it say what the contract says* (obligations written first, diff opened second, so it is not grading your own homework), *what should be gone and is it* (zero live callers, searched **by behaviour not by name**, because `materialize` was rewritten in bash and the bash did not contain the word), and *walk it across the seams* (the worked example in **both** shapes, plus the four over-engineering questions) — with a fourth science lens wherever a default moves. Findings are **fixed, deferred with an owner, or withdrawn with the premise named**; none is dropped. Four standing guards make *"what done looks like"* executable, three of them red today and each naming the phase that turns it green. **The web is the last two phases and it is two tabs, not one** — a generating tab per engine writes the description (P10), and **one shared, schema-driven tab** starts from a folder and fills in the per-stage values (P11), which is what every later engine inherits instead of copying |
| [`web/molview.md`](?doc=web/molview.md) | contract | The embeddable 3D structure viewer used on every tab — **two surfaces in one doc**: a plain-language **user guide** (moving/styling, the toolbar toggles, click/filter selection + amber shape-glow, measurements, region labels, trajectory playback, the Export menu) and the **developer contract** (the one ES-module door + look-up-live rule + concealed-3Dmol seal, `mount()` and the 15-key handle, the `molview.data` model + `installMolecule`/`exportFile`, the `applyOp` ops-as-data registry, the four-tier render engine, the selection store, the session-state timeline, the `/api/build/load` + `/api/modify/*` wire); § 24 points to the **VibrationView** sibling (now its own doc) |
| [`archive/2026-08-20-molview-css-namespace-plan.md`](?doc=archive/2026-08-20-molview-css-namespace-plan.md) | plan *(archived 2026-08-20 — all phases landed, guard test green; live rules: `molview.md` § 4/§ 8.1)* | **One prefix for MolView's stylesheet** (started 2026-08-01) — the module publishes 167 class names under 9 prefixes, 58 of them (35%) also defined outside it and 46 shared with the 3Dmol embed it is supposed to be independent of, so whichever sheet loads last styles the card. Everything becomes `molviewer-<area>-<part>`, spelled out rather than abbreviated, over the parts § 8.1 already names. Four phases ordered by risk (retire the dead frozen sheet · the private areas · the embed-shared names · the global ones), a guard test last, and the browser rather than the suite as the check — a stylesheet rename is invisible to stubs. Retired when the last phase lands |
| [`archive/2026-08-20-molview-and-checkpoint-ui-plan.md`](?doc=archive/2026-08-20-molview-and-checkpoint-ui-plan.md) | plan *(archived 2026-08-20 — delivered; the walk-throughs moved to `archive/2026-09-01-roadmap.md` § 0.3)* | **The checkpoint view, the view context, and a working Export** (2026-08-19, user; reviewed same day) — four asks, one plan, executed in phases. **1** the checkpoint panel stops being a fixed second column: a status-colored toggle beside the filter box swaps the file list and the run-history view in one space (delivered). **2A** the truth lane holds on every tab — restore re-enters HOLDING, transport reads back what it faithfully wrote, structure-opt's note leaves the history's key (delivered). **2B** the view context (`<owner>:ui`): switches, style, camera, frame — and, for read-only viewers only, the selection — come back on reopen, and writing them is never an edit; § 9.6's camera refusal reversed openly, its off-screen argument kept as the match guard (delivered). **3** Export rebuilt to § 11.3: one files door (`projects.molviewFiles`) handed to every mount, Data with a frame range, Image with range + resolution + webm/gif — the rows that silently did nothing now do the thing. Facts verified file:line before design; contract edits before code in every phase |
| [`archive/2026-08-21-vibration-parameter-integration-plan.md`](?doc=archive/2026-08-21-vibration-parameter-integration-plan.md) | plan | **The 22 shown-but-unread vibration parameters** (2026-08-21, user) — categorized (SCF machinery / physical model / workflow), each honored through the § 7a dresser contract or refused by name with measured reasons and literature references; the PROBED PySCF support matrix (PCM full-chain, SMD build-gated, ddCOSMO Hessian-less, symmetry safe on the already-relaxed path); the `refs` citation mechanism pinned to references.bib; the render-probe honesty gate whose open list ended EMPTY |
| [`archive/2026-08-20-spectra-migration-plan.md`](?doc=archive/2026-08-20-spectra-migration-plan.md) | plan | **The spectra migration — the vibrational calculation becomes a described job** (2026-08-20, user) — the old path is a parallel mini-framework (its own engine registry, `render_script → text`, a hand-launched `<job>.spectra.py`); the new one is an ordinary described calculation: `task.calculation = "vibration"` (the warm-file section already declares it), one `freq` stage, a `template@2` file sharing every genuinely shared PySCF item and adding the spectra-owned rows (Raman/IR/mode-selection/window), `spec_for` lifting the old emitters as its emission library, the deck still phase-writing `.spectra.json` so live-watch and every reader survive unchanged. The UI substitutes to the catalogue-rendered form + Send-to-Task-setup; the in-deck `compute_frequencies → thermo.txt` path retires (one Hessian door). Four phases: contracts → producer (workstation CLI E2E) → tab substitution → removal — **all four delivered 2026-08-21**. VibrationView (#104), transport, and Sol untouched |
| [`archive/2026-08-19-task-setup-tab-plan.md`](?doc=archive/2026-08-19-task-setup-tab-plan.md) | plan | **What is built on the Task setup tab and what is left** — the three-state open, the suffix-driven editor and the hand-over are in; T1 shape, T2 save (reusing `describe.write_description`, consuming `task.1st.json`), T3 the stage table, T4 the machine rows and T5 what has run are not. Plus the two invented-name mistakes this work made, and the rule they earn |
| [`web/modify-persistency-investigation.md`](?doc=web/modify-persistency-investigation.md) | investigation | **What the Molbuilder tab keeps, and what it drops** (2026-08-03) — molview.md § 11.3 names four things that look like saving; this tab has **six**. The fifth is *Save to project*, which is Export → Data arrived at from the other side — same pair of files, same `exportFile`, different door and a different destination — and the contract names only one of the two. The sixth is the tab's **own context**: which file is on the canvas, where it last saved, which panel was open — all in closure variables, **none of it kept**, because nothing under `modify/` writes to the workspace at all. That is not untidiness: after a genuine restore `_loadedFrom` is empty while a structure is plainly on screen, so the readout falls to *Picked:* and **the Load button re-enables against the file, inviting the user to discard the work just restored for them** — and a restore and a silent re-read of the file read identically on screen though one preserved unsaved edits and the other threw them away. § 11.2's own test (*state is the truth; what you are looking at is not*) sorts it: keep the file and the save target, drop the open panel and the form fields. The door already exists — a tag of the tab's own, exactly as `inspectors/structure.js` already does for `SHOWING_TAG` one directory away |
| [`web/audit-2026-08-05-tab-ui.md`](?doc=web/audit-2026-08-05-tab-ui.md) | audit report | **Static audit of every tab UI** (2026-08-05, HEAD `853a3f9`) — the six templates, their controllers, the modules they mount and the tab stylesheets, read against the contracts. Five groups by what they cost: **broken wiring** (VibrationView cannot mount on either page that loads it — its `molbuilder.viewer` borrow is published only by files no page loads, so `/spectrum-calculation` and `/results` render "vibration viewer unavailable"; the Spectrum tab silently has no detection chip; Transport's Generate status span is never written), **obsolete residue** (`lib/viewer/` — all five files, 6,884 lines, reachable from nothing; the transport header that says Generate is disabled when it works; 13 asset paths cited that don't exist; a runtime registry listing 12 modules of which 1 registers), **duplication** (the Analyze-chemistry card hand-pasted across three templates with the same ids, `_renderAutoDetectPanel` byte-identical twice plus a third copy, a load-from-sidebar block that was copied and has since drifted, `basename` ×3 beside the module written to prevent it, `setStatus` ×6, blob-download ×4, `<dialog>` scaffold ×3), **CSS collisions** (`spectra/style.css` redefining `.card`/`button` with different values than the shell it loads after), and **contract drift** (`/results` has the second mount route § 2.1 forbids). Each finding carries its evidence so it can be executed or dismissed without re-deriving it; nothing was changed. Task #56 |
| [`web/vibrationview.md`](?doc=web/vibrationview.md) | contract | The **VibrationView** module — the one component that **makes a molecule vibrate** and gets that animation out at publication quality (a *sibling* of MolView; the Spectrum tab is its only consumer). Written on MolView's pattern and **derived from it**: one importable name (`mount`), **no global read or written in either direction**, its **own concealed 3Dmol seal**, and no chrome at all. The design: two doors, not one (`setStructure` is the slow fact and refits, `showMode` is the fast one and does not); **async mount** so the handle is live (no readiness flag, nothing deferred); **amplitude has one home**; the free/held-still **partition is one fact** (`basis` given, held-still derived, so they cannot contradict); a mode that **does not fit its structure is refused**, never padded. Playback is counted in **frames, not hertz** — a cycle lasts a second and `fps` says how finely it is sliced, so smoothness and pace stop competing for one knob, and the phase comes from the frame number so screen and file are the *same sequence*. Plus **animation export** — GIF / WebM / **PNG-sequence zip with a self-explaining manifest**, with progress, cancellation and one capture in flight; the **two amplitudes** (display, exaggerated for legibility; physical, zero-point or thermal, computed by the tab) which **do not share a unit**, so the amplitude is stamped beside its `norm` and neither is read alone; and a **caption drawn into the canvas** (text, never a frequency — so an imaginary mode cannot be reformatted into an ordinary one) which therefore rides into every exported frame. § 14 is the test plan as rule → obligation; § 15 records what this replaces: the previous module borrowed a drawing surface from a global whose publisher no page loaded, so **it could not mount at all**. Task #19 (#104) |
| [`plans/plan.md`](?doc=plans/plan.md) | plan | **The one plan** — every open item from the nine documents it replaced, each with the file it came from and its verified state: four benchmark/monitor items, a Sol run that needs a machine, the configuration door's last three hand-joins, six front-end items (the document tier, one home per component, the per-page passes, two guards, the inspectors' sheet, and the unstarted editor module), the structure-info close-out and its parked backlog, and the documentation drift. Plus everything the roadmap and the two audits still carried: the cluster-half loop, PySCF's checkpoint globs, the wheel's missing static files, the last no-shim package, the browser walk-throughs, and four items needing a call from you. § 5a records what the fact-check found — most of the roadmap's open list had shipped |
| [`web/spectrumchart.md`](?doc=web/spectrumchart.md) | contract | The **SpectrumChart** module — the one component that **draws a vibrational spectrum and turns a click into a mode**. Written before any code moved, **built and wired on 2026-08-05** (287 lines out of a 3,671-line tab controller), and confirmed on the 36-mode benzene-dithiol result. Load-bearing rules: **the door says the cost** (§ 5.1 — selecting a mode is one recolour, where it used to recompute an 806-point envelope from 36 Lorentzians, 29,016 terms, per click); **the selection is mirrored, not owned** (§ 3 — a click leaves as an event and returns as an instruction, and `setSelected` never echoes it back); **the host owns the box** (§ 5.4 — the rule whose breach caused all three chart-sizing bugs of that day); and **a click takes the nearest mode** (§ 6.3 — one band width for every mode, floored at 8 cm⁻¹ *and* at 8 screen pixels; the earlier per-mode clamp was dropped once a click was read as a position rather than as a hit on a drawn mark, because it shrank crowded targets below a pixel). What the build found that the prose could not is listed in § 13. |
| [`web/workspace.md`](?doc=web/workspace.md) | contract | The session-persistence module — saves a tab's in-progress work so a reload or Undo brings it back: **one place**, files on the server under the project directory, written by one `persist()` call through the front door `dispatcher.js` (a second copy in the browser's own storage was removed — nothing ever restored from it, so it cost a write per edit and bought nothing). The **tag** — how several savers share one page, and the id that is remembered per tag; the public surface; **§ 2a what it is NOT**, whose first row is the boundary that has actually cost time: the **timeline is MolView's** (`lib/molview/history.js`, molview.md § 11.2) and this module has no idea a sequence exists — it stores numbered *states* and never compares two indices. The server half is `/api/workspace-storage/*` in `workspace_storage.py`, renamed 2026-08-02 from a name that said *timeline*. A *file* save is the Modify Save panel + projects door, not the workspace |
| [`web/projects.md`](?doc=web/projects.md) | contract | The projects sidebar file browser — the one door (`window.molbuilder.projects`) tabs use to browse/select/open/save files: the **selection** (the two `current_dir`/`current_file` slots; single-click=preview→`onChange`, double-click=commit→`onCommit`), the **content-blind byte layer** vs the one **content-aware `parser` door** (`openMolecule` → `/api/build/load` → `molview.data`; saving is the **files door**, `molviewFiles.save` → `/api/structure/save`, server owns the sidecar; the needs-overwrite handshake; why file-only), the **UI** (breadcrumb, tree + `⋯` menu, filter, header buttons, dialogs, the view/edit preview pop-up, the run-history checkpoint panel, layout), the full public surface, and the shipped-vs-planned split |
| [`web/presenters.md`](?doc=web/presenters.md) | contract | The Results-tab **file-viewer registry** — the switchboard that picks the right viewer for a file (`.xyz`→3D structure · `.molwatch.log`→trajectory movie · `.spectra.json`→spectrum · `.md`→markdown editor · `.log`/`.fdf`→text pane): the presenter **contract** (`name`/`displayName`/`match`/`mount` + `isResult`/`resultCategory`), the `register`/`pick`/`mount` surface + the shared `ctx` file reader, the pick→dispose→mount dispatch, thin-adapter vs heavy-engine (trajectory/spectra cores referenced), adding a viewer, and the **current → target** note (code is still classic `molbuilder.inspectors`; the ESM `presenters` rename is task #102) |
| [`web/runtime.md`](?doc=web/runtime.md) | contract | The **runtime registry** (`molbuilder.runtime` — `register(name,api)` + `whenReady(name)→promise`, the ask-don't-grab fix for the classic-`<script>`-vs-`type=module` load-order race; the five calls + the rules that bite) **and the shared building-block catalogue** (the notification bar, the discard-unsaved modal, the detection chip, the one markdown→safe-HTML renderer, path/constants helpers, the system-load strip; with one-line pointers to `form-schema.md`, the transport region-labels, and the already-ESM `xyz-io`), plus the current→target ESM note (the by-kind grouping + tasks #105/#106/#107) |
| [`web/notifications.md`](?doc=web/notifications.md) | contract | The **app-wide notification framework** (`molbuilder.notify`) — one consistent surface for system-level messages on any tab: the general **API** (`show({level,message,id?})→{dismiss}` / `clear` / `clearAll` / `list`, error/warn/info, **dedup-by-id + ×N**, runtime-registered — open to all callers), the **display** (host + CSS + JS co-located in the shared `_app_header.html` so a page can never `show()` into a missing host; live-region a11y, newest-on-top, ×, Clear-all), the **one built-in source** today (a failed save → the `molbuilder:persist-error` toast), and the **current→target** note (classic today → ESM + an auto-dismiss `ttl`, **task #105**) |
| [`web/form-schema.md`](?doc=web/form-schema.md) | contract | The engine-option **form builder** — a form is **generated, never hand-written**, and *from what* depends on the tab: the SIESTA and PySCF forms come from the **catalogue** (`catalogue_to_form_schema`, since 2026-08-15 — a parameter is on the page because `data/catalogue.template.toml` carries it), while Spectra and Transport still reflect their config dataclass (`dataclass_to_form_schema`). Also: the round-trip through `GET /api/build/schema/<engine>` → `renderForm` → `collectForm`, the four calls, the field kinds → controls, § 1a's **tag vocabulary and the rule for adding one**, and the two grouping axes — `group` picks the card, `category` the legend inside it |
| [`web/handover-procedure.md`](?doc=web/handover-procedure.md) | contract | **How a parameter tab reaches a description** — the three steps (render / write through the file layer / finish), who may write what and why, the `task.1st.json` rules, and the three states a receiving tab must handle. **Named once so every describing tab uses the same one**: Structure optimization and Spectrum both ride it (the Spectrum case landed 2026-08-20), § 2.1 states the cell-gate rule it owns (workflow § 9 gate ②), and Transport waits — it is a multi-component job and § 6 says what must be designed first |
| [`web/task-setup.md`](?doc=web/task-setup.md) | contract | **The tab that writes a description** — where it saves and the one-job-per-folder refusal, the shape it will not guess, the stage table (`varies` are the columns, `overrides` the cells), and **the machine half: one point is your choice, several is a measurement**, which is where `use_gpu` is decided and why ranks and threads can only be measured. Plus the ten operations that can each silently destroy a value, and the two files it writes |
| [`web/this-machine.md`](?doc=web/this-machine.md) | contract | **The tab that holds the secrets** — the only surface where one is typed. Named notification channels and this server's listener, on the machine rather than on a calculation, under one rule: it writes a secret and never reads one back (a webhook address *is* the credential, so it is masked too) |
| [`web/web-api.md`](?doc=web/web-api.md) | contract | The **server HTTP API** the browser calls — the shared contract + the one route catalogue: the uniform `{ok,…}`/`{ok:false,error}` **envelope** + the canonical structure wire shape (`workspace_payload`) + the client mirror (`_fetchEnvelope`); the **status-code map**; the **security posture** (CSP `default-src 'self'` / no inline JS / no CSRF token beyond same-origin+SameSite / the always-on per-IP rate limiter / the 50 MB cap); the complete `/api/*` **catalogue** grouped by domain, cross-linking each route's owner module doc; **full shapes** for the un-owned groups (app-level, build generate/validate, checkpoint, system, docs, admin, auth); a load round-trip; and the **removed-routes** list |
| [`web/ui-contract.md`](?doc=web/ui-contract.md) | contract | The cross-cutting **CSS/layout conventions** that keep every tab one consistent app: the **five stylesheet layers** (`tokens.css` → `page-shell.css` → `form-components.css` → module sheets → tab sheets) + the one-owner-per-shared-element rule; the **design tokens** (`lib/tokens.css` single source; `var(--token)` never raw hex; the `var(--token,#fallback)` embed pattern); **content-driven responsive** (`min()` grid floor, `.card-row`, the fused-card `@container`, the 641/640 dock↔drawer breakpoints); the visual-coherence conventions (`.status` severity one owner, the `[hidden]` guard gotcha); and the CSP style-allowed/script-banned boundary |
| [`web/results.md`](?doc=web/results.md) | contract | The **Results tab** — a dispatch shell that opens a finished calculation: the file **picker** (lists result-class files newest-first, auto-picks, the `fileSelected` event, Refresh re-scans), the tiny **controller** (pick → dispose → mount into one panel, the parsing-cover), the three viewers you land in (structure/trajectory/spectrum — cross-linked), what a **mounted viewer remembers** ("Refresh = open the same file again"), and what a **mounted viewer remembers** — *(the bundle-handoff card retired 2026-08-29: calculation-to-calculation passing is gone; the composite cites)* |
| [`web/trajectory.md`](?doc=web/trajectory.md) | contract | The **trajectory viewer** — a geometry-optimization run shown as a 3D movie + live convergence plots: the engine as a **data feeder** (MolView owns playback/cell/labels; the engine feeds frames + forces + the three force controls), the **four plots** (energy · max-force with the all-vs-free dual trace when atoms are frozen · SCF energy · SCF residual) + the green **convergence-target** lines (read from the run's own output) + auto-log-y, the **run badge** + the three-tier per-iter walltime line, **live polling** (`/api/watch/load` then poll `/api/watch/data` every 15 s, finish = two "finished" ticks, Refresh = clean reload), the CSV export, and the noted `error`/`errored` known-gap (crashed runs keep polling) |
| [`web/spectra.md`](?doc=web/spectra.md) | contract | The **spectra** surface — computing and reading a Raman vibrational spectrum: **how big to draw a mode, and why that is physics** (§ 4.1: the eigenvector fixes the shape, not the size — *exaggerated* is a drawing convention, *zero-point* is √(ħ/2ω) with the atoms falling as 1/√ν̃ and 1/√mass, *thermal* multiplies it by √coth(ħω/2kT) and only moves modes softer than the ~207 cm⁻¹ that room temperature is worth; the two normalisations use different units and crossing them is a silent error the backend's schema history already records), and **which atoms a mode belongs to** (§ 4.2: the furthest-moving atom is a hydrogen in almost every mode and usually carries almost none of the motion, so the panel reports each element's mass-weighted share — computed server-side because masses live in ASE, not in the browser) — plus **two surfaces, one engine** (`lib/spectra/core.js`) — the standalone **Spectrum tab** (`/spectrum-calculation`, describe→view: read-only MolView inspect card + auto-detect chemistry + the catalogue's vibration form + Send to Task setup — the deck is written by `prep`; the old `/api/spectra/render` script route retired at the migration's P3) and the **Results-tab presenter** (view a `*.spectra.json`); the **chart** (frequency cm⁻¹ vs Raman activity Å⁴/amu · Lorentzian broadening · red imaginary modes · density-mode fallback), the sortable **mode table** + excited-state columns, **click-a-mode → VibrationView** 3D animation (a *different* viewer from the MolView inspect card), the **render vs load** doors, **2 s live polling**, and the current→target ESM note (engine + presenter still classic) |
| [`web/bench-summary.md`](?doc=web/bench-summary.md) | contract | The **bench summary** — a whole sweep on one page while it is still running: the comparison chart across trials (`s/iter` against the coordinate the sweep varied) plus a card per trial carrying its state, its headline measurement, and what it **asked** for beside what it **ran**.  A presenter that claims `job-set.json`, not a mode of the tab (§ 1); it **composes and never recomputes** (§ 2, B1/B2) — every figure comes through the one door that already owns it (`summarize.parse_point`, `runstatus.jobset_status`, `choose_winner` for the verdict), because a page comparing six trials otherwise has six chances to repeat the "170 minutes for five 38-minute jobs" defect |
| [`web/tabs.md`](?doc=web/tabs.md) | contract | **How the six tab pages compose the modules** (the consumer doc): the **tab roster + shared shell** (canonical `TABS` order, Molbuilder is the landing tab, the injected nav + projects sidebar); one section per page — **Molbuilder** (`/molbuilder`: MolView mounted by `selection-bootstrap`, ops via `applyOp`, and the **structure-generation in-gate** `loadIntoCanvas` — SMILES/name/DNA/RNA/peptide/upload), **Structure optimization** (read-only MolView + the two engines' catalogue forms + Send-to-Task-setup; script generation left the tab 2026-08-15), **Transport** (a working TranSIESTA generator with a **current-vs-planned** box — the "placeholder" comments are stale; no in-app save yet), **Documents** (the `?doc=` reader that serves *these* docs); plus the two cross-cutting protocols — the **save out-gate** (`/api/structure/save`, server writes the `.xyz`+sidecar pair, 409 handshake) and the **data-coherence rule** (viewer-is-truth; labels validated against `n_atoms` server-side); ends on the tab-controller ESM posture |
| [`web/overview.md`](?doc=web/overview.md) | overview | **The web map** — start here: the **module-independence doctrine** (concealed, physically-separated, uniformly-reusable ES modules, with MolView as the exemplar — one module mounted read-only on 3 tabs + editable on 2), a **layers diagram** (tabs → modules → server), the **two universal patterns** (the `{ok,…}` envelope + the runtime registry), the **module registry** (every module → its own doc + a verified ESM status), the **ESM scorecard** (molview/vibrationview/workspace/projects/xyz-io full; trajectory/transport hybrid; presenters/spectra/results/form-schema/runtime classic → tasks #102/#103), and a "where to start reading" index by concern |
| [`ops/getting-started.md`](?doc=ops/getting-started.md) | guide | **From a fresh clone to a first calculation** (2026-08-28, user ask: a new user must see every file that has to exist before things start) — the one-look map of the six setup surfaces and who writes each: the clone + envs (`scripts/install-env.sh bootstrap`), the hand-written `molbuilder.json` (direct vs submit, default queue, the projects tree), the probed machine record, the remote pair (probe there, copy here — the calculation folder itself never names a machine), the server verbs with TLS + opt-in sign-in only when shared, and the notification split (the *when* travels in `task.json`, the *where* + key stay in the run machine's own config). Every step is a pointer to its owning document, never a second copy (`workflow.md` R-W1) |
| [`ops/installation.md`](?doc=ops/installation.md) | guide | **Installing molbuilder + its science backends** — the **per-backend conda-env model** (host `molbuilder` + `molbuilder-siesta`/`-pySCF`/`-MDtools`/`-siesta-gpu`, dispatched via `conda run -n`, because the backends' pins are mutually unsatisfiable), the **one install path** (`scripts/install-env.sh bootstrap` → `molbuilder envs …`; run via `python -m molbuilder`, not pip-installed), the **backends table** (what lives where + how installed; X3DNA/pyberny manual), and the **GPU-SIESTA build appendix** (ELPA-tarball + SIESTA-cmake from source, the NFS-shmem hook, driver-optional). Reconciled to `recipes.py` — legacy-README drift corrected |
| [`ops/access-control.md`](?doc=ops/access-control.md) | contract | **Who reaches this server, and what they may do** — the four gates and why they are four and not one: **identity** (opt-in SSO, molbuilder holds no passwords and learns only an email; `allowed_users` is PER PROVIDER; the short public-endpoint list; a browser gets a redirect and `/api/*` gets a JSON 401), **hostility** (the always-on per-IP limiter — signature · 404-storm · total-burst-off-by-default; allowlisted IPs and logged-in sessions skip it for *different* reasons; **only 4xx counts**, which is what makes ~170 revalidating assets per page load free), **admin** (its own top-level `admin` section — ONE list, read the same way by the block-list routes and the restart route; **absent or empty means anyone who signed in**, which is already a named set because per-provider `allowed_users` is required, and naming addresses here narrows it), and **stopping the process** (the reload route is **absent, not refused** — 404 — unless supervised AND named). Ends on the six rules underneath (**the safe state is the one you get by doing nothing**; absent beats refused; identity is borrowed never stored; judge behaviour not people; one question one gate; name the cost before doing it), the sharp edges (**TLS ≠ auth**; **expired-session 401s no longer self-block the user** — the sign-in check is not evidence; and the signature check does not run on a real page when auth is on, pinned as a known shape; the admin API needs auth on; all state is per process), and a test map |
| [`archive/2026-08-19-server-reload-plan.md`](?doc=archive/2026-08-19-server-reload-plan.md) | plan | **Seeing a code change without a manual restart** (2026-08-03; **complete — A–E landed**; shipped behaviour is in [`deployment.md`](?doc=ops/deployment.md) § 1 and § 4) — two halves that must not get one answer. **JS/CSS were never stale on the server**: Flask cached them in the BROWSER for 12 hours with no check, which is the whole reason a front-end change looked like it needed a restart. Now they revalidate (`no-cache` → 304, no body) — **not** the URL-version this plan first proposed, which reaches only the 119 `url_for` references and would have left the **51 ESM imports written inside the JavaScript** on cached copies. **Python** still needs a fresh interpreter, and a file watcher is rejected on purpose: Werkzeug stat-polls every imported module each second and fires on *any* mtime change, so a chunked write or a `git checkout` reloads against a half-written tree. What is kept from the reloader is the **process shape** — a supervising parent (default since 2026-08-04; `--no-supervise` for when systemd/Docker/gunicorn already owns restarts) that never imports application code and respawns a child on a sentinel exit, which a route re-execing itself cannot give itself. **The gate, decided:** with no supervisor the route is **absent (404), not refused (403)**, so a deployment that could not come back reads as *the button is missing* rather than as a policy. WHO may press it is a separate question with its own answer — by default anyone who signed in, since the required per-provider `allowed_users` already named them |
| [`ops/deployment.md`](?doc=ops/deployment.md) | guide | **Running the molbuilder web server** — `molbuilder serve` is a single-process **dev server** (no `--workers`; production = a reverse proxy in front terminating TLS + the **bind guard**: loopback-or-TLS); **opt-in SSO auth** (Google/GitHub/MS/ORCID/CAS — no passwords, an `allowed_users` allowlist, a session key with one home); the **security posture** (CSP `script-src 'self'`/no-inline-JS, HSTS-on-HTTPS, 50 MB cap, no CSRF token); the **rate limiter** (the threat model `web-api.md` defers here — 3 signals: attack-string signature · 404-storm · total-burst-off-by-default, 1 h cooldown, 429, admin routes); and **`molbuilder.json`** config (one location, the config directory). Reconciled to `cli.py`/`app.py`/`auth.py`/`rate_limit.py` |
| [`process/package-layout.md`](?doc=process/package-layout.md) | reference | **Where everything lives** — a domain-keyed map of the repo/package (model/science backends · engines · execution · web · ops), each area cross-linked to its owning doc; the top-level tree, the `molbuilder/` subpackages, the `web/` server+client (incl. every `static/lib/` module → its web doc), the flat-plus-subdir `tests/` layout, and packaging. Rewritten from the real tree (the legacy L1/L2/L3 folder story was stale) |
| [`process/conventions.md`](?doc=process/conventions.md) | reference | **How you write + invoke the code** — the **enforced** conventions first (L1/L2/L3 layering → `test_layering.py`; parse-layer no-I/O; pyflakes + full-suite **pre-commit**, no CI; `pythonpath` not `pip install -e`), each with its guard test; the module-provenance header flagged **advisory** (no guard); then the **CLI surface** — the thin-shell-over-the-web-API doctrine, the 13-command + 7-group catalogue (an index cross-linking execution/ops/tabs), and the exit-code / stdout-stderr / dataclass-`--help` conventions |
| [`process/testing.md`](?doc=process/testing.md) | reference | **How the project tests itself** — the **pyramid** (unit/module/interface/integration + smoke/e2e/slow markers), the **layering** structural invariant, **design-tests-around-the-envs**, testing the front-end **JS in Node without a browser** (the `_node_esm.py` harness that survives the ESM migration), the durable **Playwright** patterns (locate-what-the-user-clicks, state-based waits, viewport, `force=True` pitfalls, diagnostics), the source-text-invariant + state-composition patterns, and the pre-commit gate |
| [`process/code-audit.md`](?doc=process/code-audit.md) | reference | **The code-audit playbook** — how to run a systematic review: the audit **principles** (trust-but-verify, structural-audits-miss-behavioural-bugs, count-honestly, propagate-to-siblings, graduate-invariants-into-tests), the **dimensions**, the **five known traps** (`[hidden]`-precedence · per-feature option traversal · cross-source-of-truth gap · orphaned listener · same-shape-different-name endpoints) each with a DevTools smoking-gun diagnostic, and the per-dimension checklists — several invariants now enforced by source-text tests |
| [`process/screenshots.md`](?doc=process/screenshots.md) | process | **The README screenshot manifest** — the capture guide for the 10 PNGs in `img/`: the one-project demo convention (`projects/BDT/`, so the README reads as one continuous Au–BDT–Au story), the pre-capture setup (window size, theme, no DevTools), a per-image table (URL · what to load · zoom region · what it must communicate), and the re-capture cadence. Carries the known-stale flag on the two nav shots (five tabs captured; eight ship) |
| [`archive/MIGRATION.md`](?doc=archive/MIGRATION.md) | index | The migration ledger: every old_docs file → target home + status (closed — migration complete 2026-07-28) |
| [`archive/README.md`](?doc=archive/README.md) | index | The archive's own index: what was archived when, and what superseded it |
